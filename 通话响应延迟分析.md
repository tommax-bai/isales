# iSales 通话响应延迟分析报告

> 分析对象：`isales-engine`（实时通话引擎）+ `isales-common`（Provider ABC / RTC）
> 分析日期：2026-05-30
> 结论先行：**延迟主要来自"串行 LLM 调用 + 非流式 LLM + 垫词被前置阻塞"三件事叠加**，而不是单一环节。

---

## 一、完整响应链路（用户说完话 → 听到 AI 回复）

代码主干在 `isales-engine/isales_engine/run_loop.py` 的 `_main_turn_loop()`（225-545 行）。一轮对话的真实执行顺序：

```
用户说话
  └─ ASR 流式识别 (asr_volcengine.py, WebSocket)
       └─ 出现 is_final → 放入 asr_finals_q
            └─ _await_user_or_silence() 立即返回 user_final        [run_loop.py:592-600]
                 ├─ append_event("user_speech")                    [run_loop.py:352]
                 ├─ ★ evaluate_transfer()  ← 最多 2 次串行 LLM 调用 [run_loop.py:366]  ❗❗
                 ├─ filler.start()  ← 此时才开始垫词（要先跑 TTS）  [run_loop.py:434]  ❗
                 ├─ ★ run_pipeline() ← 3 层串行 LLM（角色→裁判→润色）[run_loop.py:436]  ❗❗❗
                 ├─ await filler.wait_finished() ← 等垫词整段放完     [run_loop.py:445]  ❗
                 └─ _play_tts(reply) ← TTS 合成 + 推流播放           [run_loop.py:454]
```

**用户从"说完"到"听到任何声音"，要先等 `evaluate_transfer` 的串行 LLM 调用跑完、再等垫词 TTS 出首字节。** 垫词本来是用来"掩盖延迟"的，结果它自己被堵在了延迟后面。

---

## 二、按严重程度排序的延迟点

### 🔴 P0-A：垫词被 `evaluate_transfer` 的串行 LLM 调用前置阻塞

**位置**：`run_loop.py:366-372`（transfer）在 `:434`（filler.start）**之前**。

```python
decision = await evaluate_transfer(           # run_loop.py:366  —— 先跑
    user_text=user_text, ...
    llm=providers.llm,
)
...
filler = FillerManager(...)
await filler.start()                          # run_loop.py:434  —— 后跑
```

`evaluate_transfer`（`transfer/manager.py:58-99`）在 keyword/round 未命中时，会**串行**发起两次 LLM 调用：
- `_call_intent`（意图识别，`intent_timeout_s=5.0`）
- `_call_independent_llm`（独立判断，`llm_timeout_s=5.0`）

这两次调用都用**非流式** LLM（见 P0-B），各自要等整段 completion。**最坏情况下，用户说完话后要先静默等待两次完整 LLM 往返，垫词才开始播。** 垫词存在的全部意义（第一时间出声、掩盖思考延迟）被这段前置逻辑抵消。

**修复方向**：
1. 把 `filler.start()` 提到 `evaluate_transfer` **之前**——用户一说完立刻起垫词，转人工判断和垫词并行跑。
2. transfer 的 intent / 独立 LLM 判断挪到 pipeline 内部或与 pipeline 并行，不要单独占一个串行 LLM 往返。
3. 多数轮次 transfer 不会触发，却每轮都付 2 次 LLM 的钱和时间——应改为"先关键词/轮次廉价判断，LLM 判断降频或异步"。

---

### 🔴 P0-B：LLM Provider 完全非流式（无首 token 流式输出）

**位置**：`providers/llm_openai_compatible.py:88-102`。

```python
async with httpx.AsyncClient(timeout=self._timeout_s) as client:
    response = await client.post(url, headers=headers, json=payload)   # 等整段返回
```

用的是 `client.post` 一次性拿完整响应，**没有 `stream=True` / SSE 流式**。这意味着：
- 每次 LLM 调用都要等**整段 JSON completion** 生成完才返回。
- 因为管线强制 `json_mode=True`（`response_format={"type":"json_object"}`），输出是结构化 JSON，token 量不小，首字节延迟 ≈ 全量生成延迟。

由于下游（裁判、润色、TTS）都依赖上一层的**完整文本**，这里没法简单改流式，但**首 token 延迟（TTFT）被放大成了"全量生成延迟 ×3 层"**（见 P0-C）。

**修复方向**：润色层（polish，最后一层、直接产出要播的话）应改成流式输出，TTS 边收边合成（见 P0-D）；角色/裁判层因要解析完整 JSON 可保留非流式，但应缩短 `max_tokens` 和精简 JSON schema。

---

### 🔴 P0-C：AI 管线是 3 层串行 LLM 往返

**位置**：`pipeline/orchestrator.py:_run_layers()`（139-228 行）。

```
Layer 1: N 个角色 LLM（彼此并行）           [orchestrator.py:158]
   ↓ 等全部返回
Layer 2: M 个裁判 LLM（彼此并行）           [orchestrator.py:171]
   ↓ 等全部返回
Layer 3: 1 个润色 LLM                       [orchestrator.py:194]
```

层**内**是并行的（`asyncio.gather`），但层**间**是严格串行的：必须等所有角色返回 → 才发裁判 → 才发润色。所以一轮回复的管线延迟 ≈ **`max(角色耗时) + max(裁判耗时) + 润色耗时`**，是三次完整 LLM 往返串起来的。

每层超时 `pipeline_timeout_ms=8000`（`run_loop.py:83`），单层慢/卡可拖到 8 秒，三层叠加最坏 24 秒。

**修复方向**：
- 评估是否每轮都需要完整三层。简单寒暄/确认类回复可走"单角色直出"快路径，跳过裁判+润色。
- 裁判层可与角色层流水化（角色一旦产出就送裁判），而不是等全部角色 barrier。
- 缩短各层 `max_tokens`、用更快的模型档位跑裁判（裁判只需 yes/no）。

---

### 🟠 P1-A：垫词 `wait_finished()` 在回复 TTS 前阻塞

**位置**：`run_loop.py:444-445`。

```python
if filler is not None:
    await filler.wait_finished()      # 等垫词整段播完才往下
sm.transition_to(CallStatus.SPEAKING, ...)
played = await _play_tts(..., result.reply, ...)   # 才开始播真正回复
```

即使管线很快返回，引擎也要**等垫词音频整段播放结束**才开始播真实回复。这是 filler spec 为了"对话节奏一致"故意设计的（`filler_manager.py:6-9` 注释），但它的副作用是：**当管线比垫词快时，省下来的时间被垫词尾巴吃掉了。** 净延迟 = `max(管线耗时, 垫词时长)` 而非 `管线耗时`。

**修复方向**：允许真实回复在垫词播放中"接管"——垫词淡出/截断后立刻接回复，而不是等垫词自然播完。

---

### 🟠 P1-B：垫词走实时 TTS 合成，而非预渲染音频

**位置**：`filler_manager.py:181-196`。

```python
async def _stream_audio(self, phrase):
    async def chunks():
        async for chunk in self._tts.synthesize_stream(phrase.text, self._voice_id):
            yield chunk
    await self._telephony.audio_out(...)
```

注释明说"Real production stage 6 will skip TTS and stream the pre-rendered audio file from OSS；stage 4 用 mock TTS"（`filler_manager.py:182-187`）。当前实现**每次垫词都现场调 TTS 合成**，垫词首字节延迟 = TTS 首字节延迟。垫词的卖点是"瞬间出声"，但现在它和真实回复一样要等 TTS。

DB 模型里 `FillerPhrase` 已经有 `audio_url` + `generation_status=READY` 字段（`filler_manager.py:128-132` 在筛选 ready 的 phrase），说明**预渲染音频的设计已存在但播放路径没接上**——`_stream_audio` 应该直接从 `phrase.audio_url`（OSS）拉预渲染 PCM 流，跳过 TTS。

**修复方向**：垫词播放改为直接流式拉取 `audio_url` 的预渲染 PCM，彻底去掉垫词路径上的 TTS 合成延迟。

---

### 🟠 P1-C：每次 LLM / TTS 调用新建 httpx client，无连接复用

**位置**：`llm_openai_compatible.py:90`、`tts_volcengine.py:124`。

```python
# 每次 chat() 都新建
async with httpx.AsyncClient(timeout=self._timeout_s) as client:
    response = await client.post(...)
# 每次 synthesize_stream() 都新建
async with httpx.AsyncClient(timeout=self._timeout_s) as client, client.stream(...):
```

每次调用都新建 `AsyncClient` → 退出即销毁连接池，**无法复用 TCP 连接和 TLS 会话**。一轮对话里有 角色×N + 裁判×M + 润色 + transfer×2 次 LLM 调用，每次都付一遍 TCP 握手 + TLS 握手（跨公网到火山/OpenAI，单次握手几十~上百 ms）。**N=3、M=2 时一轮就是约 10 次握手的浪费。**

**修复方向**：Provider 持有一个长生命周期的 `httpx.AsyncClient`（连接池 + keepalive），在 `__init__` 建、实例销毁时关；同 host 的多次调用复用连接。这是低风险高收益的改动。

---

### 🟡 P2-A：单层超时 8s，无更细粒度的快速失败

`pipeline_timeout_ms=8000`（`run_loop.py:83`）作用在每一层。某个角色/裁判 LLM 卡住时，整层要等到 8s 才超时降级。三层最坏 24s。transfer 的 intent/independent 调用各自 5s 超时（`transfer/manager.py:65-66`），也都串在主路径上。

**修复方向**：按层设差异化超时（裁判这种轻判断给 2-3s）；首 token 监控，长时间无响应提前切默认回复。

### 🟡 P2-B：润色失败回退链

`orchestrator.py:200-214` 润色超时/解析失败 → 回退到第一个通过的候选。这本身合理（属于协议边界的 fallback），但叠加在前面的串行延迟上，意味着"等满 8s 超时 → 再回退"。属于 CLAUDE.md 提到的"多层 fallback"信号，值得连同 P0-C 一起重审管线是否过度设计。

---

## 三、各环节延迟归因小结表

| 环节 | 代码位置 | 延迟性质 | 严重度 |
|---|---|---|---|
| ASR 识别完成→处理 | `run_loop.py:592-600` | 拿到 final 立即返回，**本身不阻塞**；端点延迟取决于火山 ASR 的 is_final 时机 | ✅ 良好 |
| transfer 串行 2× LLM | `run_loop.py:366` / `transfer/manager.py:86-99` | 前置阻塞垫词，每轮 2 次非流式往返 | 🔴 P0 |
| LLM 非流式 | `llm_openai_compatible.py:91` | 等整段 completion，无 TTFT 优势 | 🔴 P0 |
| 管线 3 层串行 | `orchestrator.py:158/171/194` | 3 次 LLM 往返串联 | 🔴 P0 |
| 垫词被前置阻塞 | `run_loop.py:366 在 434 之前` | 垫词起播被 transfer LLM 推后 | 🔴 P0 |
| 垫词 wait_finished | `run_loop.py:445` | 回复被垫词尾巴阻塞 | 🟠 P1 |
| 垫词走实时 TTS | `filler_manager.py:181` | 预渲染音频未接通 | 🟠 P1 |
| httpx 无连接复用 | `llm_*.py:90` / `tts_*.py:124` | 每次调用重握手 | 🟠 P1 |
| TTS 合成→播放 | `tts_volcengine.py` + `run_loop.py:_play_tts` | TTS **本身是流式**（`aiter_bytes`），推流也有背压；这一段相对健康 | ✅ 较好 |
| RTC 推流 | `aliyun_rtc.py:203` push_audio | 有 drain 背压，按 SDK 缓冲走，**不是实时逐帧 sleep**，不串行化 | ✅ 良好 |

---

## 四、建议的优先修复顺序（说人话）

**最该先改的三件事，预计能砍掉一大半"用户说完到听到声"的延迟：**

1. **把垫词提到转人工判断前面**——用户一说完立刻出垫词声，别让它排在两次 LLM 判断后面。（改 `run_loop.py` 几行顺序，风险最低，收益最直接）
2. **垫词改放预渲染音频**——别每次现场合成 TTS，DB 里 `audio_url` 字段已经留好了，直接拉来播。
3. **Provider 复用 HTTP 连接**——别每次调 LLM/TTS 都重新握手，一轮对话能省近十次握手。

**接着评估**：3 层管线是不是每轮都必须跑满，简单回复能不能走"单角色直出"快路径；以及最后的润色层能不能改成边生成边给 TTS 播。

> ⚠️ 注意：以上 1-4 项里，第 4 项（管线裁剪 / 润色流式）改的是 **AI 管线行为**，按本仓约定属于业务/架构改动，需走 OpenSpec 流程（`/opsx:propose`）。第 1-3 项是纯工程实现优化（顺序调整 / 连接复用 / 接通已有字段），可直接改代码。
