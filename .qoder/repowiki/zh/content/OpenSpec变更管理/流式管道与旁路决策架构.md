# 流式管道与旁路决策架构

<cite>
**本文档引用的文件**
- [DESIGN.md](file://DESIGN.md)
- [README.md](file://README.md)
- [ai-pipeline/spec.md](file://openspec/specs/ai-pipeline/spec.md)
- [call-state-machine/spec.md](file://openspec/specs/call-state-machine/spec.md)
- [architecture/spec.md](file://openspec/specs/architecture/spec.md)
- [service-communication/spec.md](file://openspec/specs/service-communication/spec.md)
- [transcript/spec.md](file://openspec/specs/transcript/spec.md)
- [data-model/spec.md](file://openspec/specs/data-model/spec.md)
- [message-contract/spec.md](file://openspec/specs/message-contract/spec.md)
- [pipeline-stream-and-referee/design.md](file://openspec/changes/pipeline-stream-and-referee/design.md)
- [pipeline-stream-and-referee/proposal.md](file://openspec/changes/pipeline-stream-and-referee/proposal.md)
- [pipeline-stream-and-referee/specs/data-model/spec.md](file://openspec/changes/pipeline-stream-and-referee/specs/data-model/spec.md)
- [pipeline-stream-and-referee/specs/ai-pipeline/spec.md](file://openspec/changes/pipeline-stream-and-referee/specs/ai-pipeline/spec.md)
- [pipeline-stream-and-referee/specs/goal-achievement/spec.md](file://openspec/changes/pipeline-stream-and-referee/specs/goal-achievement/spec.md)
- [pipeline-stream-and-referee/specs/provider-abc/spec.md](file://openspec/changes/pipeline-stream-and-referee/specs/provider-abc/spec.md)
- [pipeline-stream-and-referee/specs/role-prompt/spec.md](file://openspec/changes/pipeline-stream-and-referee/specs/role-prompt/spec.md)
- [engine/queue_consumer.py](file://engine/queue_consumer.py)
- [engine/state_machine.py](file://engine/state_machine.py)
- [engine/pipeline/orchestrator.py](file://engine/pipeline/orchestrator.py)
- [engine/realtime/interruption_detector.py](file://engine/realtime/interruption_detector.py)
- [engine/realtime/silence_detector.py](file://engine/realtime/silence_detector.py)
- [engine/transfer/manager.py](file://engine/transfer/manager.py)
- [engine/wrapup/manager.py](file://engine/wrapup/manager.py)
- [engine/transcript_recorder.py](file://engine/transcript_recorder.py)
- [engine/telephony_client.py](file://engine/telephony_client.py)
- [isales-common/enums.py](file://isales-common/isales_common/enums.py)
</cite>

## 更新摘要
**所做更改**
- 新增sentence splitter模块，实现流式token到句子的智能切分
- 完善referee旁路决策系统，支持实时结构化决策和fail-open机制
- 增强LLM provider流式支持，新增chat_stream接口和JSON模式约束
- 实现post-call extractor异步处理架构，通过Redis队列解耦处理
- 从三层judge+polish架构完全重构为双LLM架构，显著降低首音频延迟
- 更新RoleKind枚举，从{role,judge,polish}改为{main,referee,extractor}

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)

## 简介

iSales 是一个基于 OpenSpec 规范的 AI 外呼销售系统，采用"流式管道与旁路决策架构"设计。该架构通过双LLM架构实现高效的实时通话处理，其中主LLM负责流式对话生成，旁路referee LLM负责结构化决策，post-call extractor负责客户信息抽取。系统采用7仓库微服务架构，包括 isales-common、isales-api、isales-engine、isales-scheduler、isales-worker、isales-telephony 和 isales-web，通过 Redis 队列和 Pub/Sub 实现服务间通信。

**更新** 从三层judge+polish架构完全重构为双LLM架构，新增sentence splitter模块、referee旁路决策系统、LLM provider流式支持增强、post-call extractor异步处理等新功能，显著降低首音频延迟并提升整体性能。

## 项目结构

```mermaid
graph TB
subgraph "iSales 系统架构"
subgraph "前端层"
WEB[Vue 3 前端]
end
subgraph "管理服务层"
API[管理 API + WS 代理]
WEB_ADMIN[Web 管理界面]
end
subgraph "核心引擎层"
ENGINE[实时通话引擎]
SCHEDULER[调度器]
WORKER[后台工作者]
end
subgraph "通信层"
REDIS[Redis 队列/Pub/Sub]
HTTP[HTTP 通信]
end
subgraph "硬件层"
TELEPHONY_API[Telephony API]
MODEM_CONTROLLER[Modem 控制器]
GSM_MODEM[GSM Modem × ≤8]
end
subgraph "共享库"
COMMON[isales-common 共享库]
end
WEB_ADMIN --> API
API --> REDIS
API --> HTTP
SCHEDULER --> REDIS
ENGINE --> REDIS
WORKER --> REDIS
ENGINE --> HTTP
TELEPHONY_API --> MODEM_CONTROLLER
MODEM_CONTROLLER --> GSM_MODEM
COMMON -.-> API
COMMON -.-> ENGINE
COMMON -.-> SCHEDULER
COMMON -.-> WORKER
COMMON -.-> TELEPHONY_API
end
```

**图表来源**
- [architecture/spec.md:130-168](file://openspec/specs/architecture/spec.md#L130-L168)
- [service-communication/spec.md:11-24](file://openspec/specs/service-communication/spec.md#L11-L24)

**章节来源**
- [README.md:1-86](file://README.md#L1-L86)
- [architecture/spec.md:1-168](file://openspec/specs/architecture/spec.md#L1-L168)

## 核心组件

### 状态机核心组件

系统的核心是统一的状态机，负责管理通话生命周期的所有状态转换：

```mermaid
stateDiagram-v2
[*] --> INIT : 拨号尝试
INIT --> GREETING : connected 事件
GREETING --> LISTENING : 开场白完成
LISTENING --> PROCESSING : 用户说话结束
LISTENING --> ACTIVATING : 沉默激活阈值
LISTENING --> TRANSFERRING : 转人工触发
PROCESSING --> SPEAKING : 生成回复
PROCESSING --> WRAPPING_UP : 目标达成
PROCESSING --> INTERRUPED : 用户打断
SPEAKING --> LISTENING : TTS 播放完成
SPEAKING --> INTERRUPED : 用户打断
INTERRUPED --> PROCESSING : 继续处理
INTERRUPED --> LISTENING : 恢复监听
ACTIVATING --> LISTENING : 激活完成
WRAPPING_UP --> END : 收尾完成
TRANSFERRING --> END : 主动挂断
[*] --> END : 通话结束
```

**图表来源**
- [call-state-machine/spec.md:7-21](file://openspec/specs/call-state-machine/spec.md#L7-L21)
- [call-state-machine/spec.md:46-149](file://openspec/specs/call-state-machine/spec.md#L46-L149)

### 双LLM管道组件

AI 管道采用双LLM并行处理架构，主LLM负责流式对话，referee负责结构化决策：

```mermaid
flowchart TD
START[进入 PROCESSING 状态] --> MAIN[主LLM流式生成回复]
MAIN --> SPLITTER[sentence_splitter切句]
SPLITTER --> REFERENCE[旁路referee LLM并行决策]
REFERENCE --> TTS[TTS合成与播放]
TTS --> SPEAKING[SPEAKING状态]
SPEAKING --> DECISION{referee决策}
DECISION --> |goal_achieved| WRAPPING_UP[进入收尾状态]
DECISION --> |transfer| TRANSFERRING[转人工状态]
DECISION --> |customer_decline| ACTIVATING[激活话术]
DECISION --> |continue| LISTENING[继续监听]
WRAPPING_UP --> END[通话结束]
TRANSFERRING --> END
ACTIVATING --> LISTENING
LISTENING --> PROCESSING[新一轮PROCESSING]
```

**图表来源**
- [pipeline-stream-and-referee/design.md:75-98](file://openspec/changes/pipeline-stream-and-referee/design.md#L75-L98)

**更新** 新增sentence splitter模块，实现流式token到句子的智能切分；从三层judge+polish架构重构为双LLM架构，主LLM输出纯文本流式回复，referee并行执行结构化决策。

**章节来源**
- [pipeline-stream-and-referee/design.md:1-278](file://openspec/changes/pipeline-stream-and-referee/design.md#L1-L278)
- [pipeline-stream-and-referee/specs/ai-pipeline/spec.md:73-94](file://openspec/changes/pipeline-stream-and-referee/specs/ai-pipeline/spec.md#L73-L94)

## 架构概览

### 服务间通信架构

系统采用统一的消息契约和通信协议：

```mermaid
graph LR
subgraph "消息契约层"
MSG[消息契约定义<br/>Pydantic 模型]
VERSION[版本控制<br/>schema_version]
TRACE[追踪标识<br/>message_id]
end
subgraph "通信通道层"
QUEUE[Redis 队列<br/>可靠投递]
PUBSUB[Redis Pub/Sub<br/>实时广播]
HTTP[HTTP 调用<br/>同步请求]
end
subgraph "服务层"
SCHEDULER[调度器]
ENGINE[通话引擎]
WORKER[后台工作者]
API[管理 API]
TELEPHONY[Telephony API]
end
MSG --> VERSION
MSG --> TRACE
MSG --> QUEUE
MSG --> PUBSUB
MSG --> HTTP
SCHEDULER --> QUEUE
ENGINE --> QUEUE
WORKER --> QUEUE
API --> PUBSUB
ENGINE --> PUBSUB
API --> QUEUE
SCHEDULER --> HTTP
```

**图表来源**
- [message-contract/spec.md:6-85](file://openspec/specs/message-contract/spec.md#L6-L85)
- [service-communication/spec.md:7-34](file://openspec/specs/service-communication/spec.md#L7-L34)

### 数据存储架构

采用统一的数据模型和存储策略，包含新的双LLM架构数据结构：

```mermaid
erDiagram
CALL_RECORD {
uuid id PK
uuid lead_id FK
uuid campaign_id FK
string caller_id
string status
timestamp started_at
timestamp ended_at
integer duration
jsonb transcript
string recording_url
string transfer_status
string wrap_up_started_at
jsonb prompt_versions
jsonb extracted
string extract_status
string extract_error
}
PIPELINE_TRACE {
uuid id PK
uuid call_record_id FK
integer turn_id
timestamp ts_start
timestamp ts_end
string user_input
string main_reply_text
integer main_duration_ms
integer main_tokens_in
integer main_tokens_out
boolean main_fallback_used
string referee_decision
string referee_goal_type
float referee_confidence
integer referee_duration_ms
integer first_audio_ms
string error
}
CAMPAIGN {
uuid id PK
string name
uuid voice_id FK
jsonb default_replies
integer concurrency
jsonb time_windows
jsonb extraction_fields
integer max_silence_activations
integer silence_threshold_ms
jsonb silence_phrases
string silence_hangup_phrase
integer max_no_progress_seconds
integer wrap_up_max_rounds
integer wrap_up_max_seconds
jsonb wrap_up_closing_phrases
jsonb interruption_whitelist
integer interruption_min_duration_ms
integer max_continuous_interruptions
string continuous_interruption_strategy
boolean transfer_keyword_enabled
jsonb transfer_keywords
boolean transfer_intent_enabled
float transfer_intent_threshold
boolean transfer_round_enabled
float transfer_round_threshold
boolean transfer_llm_enabled
uuid transfer_llm_prompt_version_id
jsonb transfer_phrases
jsonb retry_intervals
integer retry_max_count
integer follow_up_interval_days
integer follow_up_max_count
boolean do_not_call_keywords_enabled
boolean do_not_call_llm_enabled
uuid do_not_call_llm_prompt_version_id
boolean respect_holidays
}
ROLE_CONFIG {
uuid id PK
uuid campaign_id FK
string kind
string model
uuid current_prompt_version_id FK
float temperature
float top_p
jsonb ext_params
boolean enabled
}
CALL_RECORD ||--|| PIPELINE_TRACE : "包含"
CAMPAIGN ||--o{ ROLE_CONFIG : "拥有"
```

**图表来源**
- [pipeline-stream-and-referee/specs/data-model/spec.md:38-57](file://openspec/changes/pipeline-stream-and-referee/specs/data-model/spec.md#L38-L57)

**更新** 新增extract_status、extract_error等字段，删除judge_results、polish_*相关字段。

**章节来源**
- [service-communication/spec.md:78-150](file://openspec/specs/service-communication/spec.md#L78-L150)
- [pipeline-stream-and-referee/specs/data-model/spec.md:1-83](file://openspec/changes/pipeline-stream-and-referee/specs/data-model/spec.md#L1-L83)

## 详细组件分析

### 状态机组件分析

状态机作为系统的核心协调器，实现了统一的状态转换管理和事件驱动机制：

```mermaid
classDiagram
class CallSession {
+uuid id
+CallState current_state
+dict context
+transition_to(state, reason)
+validate_transition(event)
+record_event(event)
}
class StateMachine {
+dict LEGAL_TRANSITIONS
+process_event(event)
+handle_illegal_transition(event)
+maintain_consecutive_interruption_count()
}
class EventPublisher {
+publish_engine_event(event)
+subscribe_campaign(campaign_id)
+unsubscribe_campaign(campaign_id)
}
class TranscriptRecorder {
+write_pipeline_trace(trace_data)
+batch_write(transcripts)
+retry_with_backoff(max_attempts)
}
CallSession --> StateMachine : "协调"
StateMachine --> EventPublisher : "发布事件"
StateMachine --> TranscriptRecorder : "记录转录"
StateMachine --> CallSession : "管理状态"
```

**图表来源**
- [engine/state_machine.py](file://engine/state_machine.py)
- [engine/call_session.py](file://engine/call_session.py)
- [engine/transcript_recorder.py](file://engine/transcript_recorder.py)
- [engine/event_publisher.py](file://engine/event_publisher.py)

### 双LLM管道组件分析

AI 管道实现了复杂的并行处理和旁路决策机制：

```mermaid
sequenceDiagram
participant SM as 状态机
participant Orchestrator as 编排器
participant MainLLM as 主LLM
participant Splitter as Sentence Splitter
participant RefereeLLM as Referee LLM
participant TTS as TTS合成器
participant Recorder as 记录器
SM->>Orchestrator : 进入 PROCESSING
Orchestrator->>MainLLM : 并行流式生成回复
MainLLM-->>Splitter : 返回文本片段
Splitter-->>Orchestrator : 返回完整句子
Orchestrator->>RefereeLLM : 并行结构化决策
RefereeLLM-->>Orchestrator : 返回决策结果
Orchestrator->>TTS : 合成并播放回复
TTS-->>Orchestrator : 播放完成信号
Orchestrator->>Recorder : 写入 pipeline_trace
Orchestrator-->>SM : 返回referee决策
SM-->>SM : 状态转换
```

**图表来源**
- [engine/pipeline/orchestrator.py](file://engine/pipeline/orchestrator.py)
- [pipeline-stream-and-referee/design.md:75-98](file://openspec/changes/pipeline-stream-and-referee/design.md#L75-L98)

**更新** 新增sentence splitter模块，实现流式token到句子的智能切分；从三层judge+polish架构重构为双LLM架构，主LLM输出纯文本流式回复，referee并行执行结构化决策。

### 实时检测组件分析

系统实现了多种实时检测机制来处理通话过程中的各种情况：

```mermaid
flowchart TD
DETECT[实时检测] --> INTERRUP[打断检测]
DETECT --> SILENCE[沉默检测]
DETECT --> TRANSFER[转人工检测]
INTERRUP --> INTERRUP_RULES[白名单 + 时长条件]
INTERRUP_RULES --> INTERRUP_ACTION[打断处理]
SILENCE --> SILENCE_THRESH[阈值检测]
SILENCE_THRESH --> SILENCE_ACTION[激活话术]
TRANSFER --> TRANSFER_RULES[关键词/意图检测]
TRANSFER_RULES --> TRANSFER_ACTION[转人工处理]
INTERRUP_ACTION --> STATE_MACHINE[状态机转换]
SILENCE_ACTION --> STATE_MACHINE
TRANSFER_ACTION --> STATE_MACHINE
```

**图表来源**
- [engine/realtime/interruption_detector.py](file://engine/realtime/interruption_detector.py)
- [engine/realtime/silence_detector.py](file://engine/realtime/silence_detector.py)
- [engine/transfer/manager.py](file://engine/transfer/manager.py)

### Post-call Extractor组件分析

post-call extractor实现了异步客户信息抽取架构：

```mermaid
sequenceDiagram
participant Engine as 通话引擎
participant Redis as Redis队列
participant Worker as Worker服务
participant LLM as LLM Provider
Engine->>Engine : 通话结束准备END状态
Engine->>Redis : LPUSH isales : extract 任务
Redis-->>Worker : BLPOP队列任务
Worker->>LLM : chat(json_mode=True)抽取
LLM-->>Worker : 返回extracted结果
Worker->>Engine : 更新call_record.extracted
Worker->>Engine : 更新extract_status='done'
```

**图表来源**
- [pipeline-stream-and-referee/specs/ai-pipeline/spec.md:130-152](file://openspec/changes/pipeline-stream-and-referee/specs/ai-pipeline/spec.md#L130-L152)
- [pipeline-stream-and-referee/specs/service-communication/spec.md:28-48](file://openspec/changes/pipeline-stream-and-referee/specs/service-communication/spec.md#L28-L48)

**更新** 新增post-call extractor异步处理架构，通过Redis队列实现engine与extractor的完全解耦。

### Sentence Splitter组件分析

sentence splitter模块实现了智能的流式token切句：

```mermaid
flowchart TD
TOKEN_STREAM[流式token输入] --> BUFFER[累积到buffer]
BUFFER --> CHECK_PUNCT{命中标点?}
CHECK_PUNCT --> |是| YIELD[Yield完整句子]
CHECK_PUNCT --> |否| CHECK_LENGTH{超过50字?}
CHECK_LENGTH --> |是| YIELD
CHECK_LENGTH --> |否| WAIT[等待更多token]
YIELD --> RESET[重置buffer]
RESET --> BUFFER
WAIT --> BUFFER
BUFFER --> END_STREAM{流结束?}
END_STREAM --> |是| FLUSH[flush buffer]
FLUSH --> END[结束]
END_STREAM --> |否| CHECK_PUNCT
```

**图表来源**
- [pipeline-stream-and-referee/tasks.md:44-49](file://openspec/changes/pipeline-stream-and-referee/tasks.md#L44-L49)

**更新** 新增sentence splitter模块，实现流式token到句子的智能切分，支持中文标点、换行符和长度限制。

### LLM Provider流式支持分析

LLM Provider增强了流式支持和JSON模式约束：

```mermaid
flowchart TD
CHAT_STREAM[chat_stream接口] --> VENDOR[供应商SSE流]
VENDOR --> CHUNK[文本增量块]
CHUNK --> TOKEN[token/chunk]
TOKEN --> RETURN[Yield返回]
CHAT_STREAM --> JSON_MODE[JSON模式约束]
JSON_MODE --> VALIDATE[JSON格式校验]
VALIDATE --> SUCCESS[成功返回]
VALIDATE --> FAIL[异常处理]
FAIL --> FALLBACK[一次性fallback]
```

**图表来源**
- [pipeline-stream-and-referee/specs/provider-abc/spec.md:24-52](file://openspec/changes/pipeline-stream-and-referee/specs/provider-abc/spec.md#L24-L52)

**更新** 新增chat_stream接口，支持供应商SSE流协议；增强JSON模式约束，确保输出格式一致性。

## 依赖关系分析

### 组件耦合度分析

系统采用了松耦合的设计原则，通过统一的接口和消息契约实现组件间的解耦：

```mermaid
graph TB
subgraph "核心依赖关系"
STATE_MACHINE[状态机] --> PIPELINE[双LLM管道]
STATE_MACHINE --> DETECTORS[实时检测]
STATE_MACHINE --> TRANSFER[转人工]
STATE_MACHINE --> WRAPUP[收尾管理]
PIPELINE --> TRANSCRIPT_RECORDER[转录记录器]
PIPELINE --> TELEPHONY_CLIENT[通话客户端]
PIPELINE --> POST_CALL_EXTRACTOR[Post-call提取器]
PIPELINE --> SENTENCE_SPLITTER[Sentence Splitter]
PIPELINE --> REFEREE_LLM[Referee LLM]
DETECTORS --> STATE_MACHINE
TRANSFER --> STATE_MACHINE
WRAPUP --> STATE_MACHINE
STATE_MACHINE --> DATABASE[数据库]
STATE_MACHINE --> REDIS[Redis]
POST_CALL_EXTRACTOR --> WORKER_SERVICE[Worker服务]
WORKER_SERVICE --> DATABASE
WORKER_SERVICE --> REDIS
SENTENCE_SPLITTER --> PIPELINE
REFEREE_LLM --> PIPELINE
end
subgraph "外部依赖"
TELEPHONY_CLIENT --> MODEM_CONTROLLER[Modem 控制器]
MODEM_CONTROLLER --> GSM_MODEM[GSM Modem]
DATABASE --> POSTGRESQL[PostgreSQL]
REDIS --> REDIS_SERVER[Redis 服务器]
end
```

**图表来源**
- [engine/state_machine.py](file://engine/state_machine.py)
- [engine/pipeline/orchestrator.py](file://engine/pipeline/orchestrator.py)
- [engine/transcript_recorder.py](file://engine/transcript_recorder.py)
- [engine/telephony_client.py](file://engine/telephony_client.py)

### 错误处理和降级策略

系统实现了多层次的错误处理和降级机制：

```mermaid
flowchart TD
ERROR[系统错误] --> CHECK_TYPE{错误类型}
CHECK_TYPE --> |网络错误| RETRY[重试机制]
CHECK_TYPE --> |LLM 超时| FALLBACK[降级策略]
CHECK_TYPE --> |数据库错误| BUFFER[缓冲处理]
CHECK_TYPE --> |硬件错误| ISOLATE[隔离保护]
CHECK_TYPE --> |referee失效| FAIL_OPEN[Fail-open机制]
RETRY --> BACKOFF[指数退避]
BACKOFF --> MAX_RETRY{达到最大重试次数?}
MAX_RETRY --> |是| ABORT[中止处理]
MAX_RETRY --> |否| RETRY
FALLBACK --> DEFAULT_REPLY[默认回复]
DEFAULT_REPLY --> CONTINUE[继续处理]
BUFFER --> FLUSH[批量写入]
FLUSH --> CONTINUE
ISOLATE --> CLEANUP[清理资源]
CLEANUP --> SAFE_MODE[安全模式]
FAIL_OPEN --> CONTINUE
CONTINUE --> RESUME[恢复处理]
ABORT --> END_CALL[结束通话]
SAFE_MODE --> RESUME
```

**更新** 新增referee决策失败的fail-open机制，以及post-call extractor的异步处理策略；增强LLM provider的异常映射和重连机制。

**图表来源**
- [pipeline-stream-and-referee/design.md:201-206](file://openspec/changes/pipeline-stream-and-referee/design.md#L201-L206)
- [service-communication/spec.md:87-105](file://openspec/specs/service-communication/spec.md#L87-L105)

**章节来源**
- [pipeline-stream-and-referee/design.md:201-278](file://openspec/changes/pipeline-stream-and-referee/design.md#L201-L278)
- [service-communication/spec.md:87-150](file://openspec/specs/service-communication/spec.md#L87-L150)

## 性能考虑

### 并行处理优化

系统通过双层并行处理实现性能优化：

1. **双LLM并行化**：主LLM流式生成与referee结构化决策同时运行
2. **垫词与处理并行**：FILLER 状态与 PROCESSING 并行启动
3. **状态转换最小化**：通过统一的 transition_to 接口减少状态变更开销
4. **首音频延迟优化**：从6.5-9.5秒降至500-1500毫秒
5. **sentence splitter优化**：智能切句减少TTS合成等待时间

### 资源管理策略

```mermaid
flowchart LR
subgraph "并发控制"
CONCURRENT[并发计数器<br/>Redis INCR/DECR]
LIMIT[并发限制<br/>≤8 路]
QUEUE[队列管理<br/>调度器队列]
end
subgraph "内存优化"
CACHE[缓存策略<br/>Redis 缓存]
BATCH[批量处理<br/>事务批量写入]
STREAM[流式处理<br/>实时事件]
end
subgraph "网络优化"
RETRY[重试机制<br/>指数退避]
TIMEOUT[超时控制<br/>单角色级超时]
FAILOVER[故障转移<br/>备用服务]
end
CONCURRENT --> LIMIT
LIMIT --> QUEUE
CACHE --> BATCH
BATCH --> STREAM
RETRY --> TIMEOUT
TIMEOUT --> FAILOVER
```

**更新** 新增referee LLM的小模型使用策略，显著降低决策成本和延迟；优化sentence splitter的内存使用和处理效率。

**图表来源**
- [service-communication/spec.md:45-77](file://openspec/specs/service-communication/spec.md#L45-L77)
- [pipeline-stream-and-referee/design.md:9-14](file://openspec/changes/pipeline-stream-and-referee/design.md#L9-L14)

## 故障排除指南

### 常见问题诊断

1. **状态机异常**：
   - 检查 LEGAL_TRANSITIONS 配置
   - 验证事件驱动接口调用
   - 查看 state_error 事件记录

2. **双LLM管道故障**：
   - 监控 pipeline_trace 表的新字段
   - 检查 LLM Provider 状态
   - 验证主LLM流式输出和referee决策
   - 检查sentence splitter的切句效果

3. **通信问题**：
   - 检查 Redis 连接状态
   - 验证消息契约版本
   - 监控队列积压情况
   - 检查post-call extractor队列状态

4. **post-call extractor异常**：
   - 检查 worker 服务状态
   - 验证 Redis 队列订阅
   - 监控提取状态字段
   - 检查extract_status状态变化

5. **referee决策异常**：
   - 检查referee LLM配置
   - 验证JSON模式输出格式
   - 监控confidence阈值
   - 检查fail-open机制

### 调试工具和方法

```mermaid
flowchart TD
ISSUE[发现问题] --> LOGS[查看日志]
LOGS --> EVENTS[检查事件流]
EVENTS --> TRACE[分析 pipeline_trace]
TRACE --> REPRODUCE[重现问题]
REPRODUCE --> FIX[实施修复]
FIX --> VERIFY[验证修复]
VERIFY --> MONITOR[持续监控]
LOGS --> STATE_MACHINE[状态机日志]
LOGS --> PIPELINE[管道日志]
LOGS --> COMMUNICATION[通信日志]
LOGS --> EXTRACTOR[Extractor日志]
EVENTS --> TRANSITION[状态转换事件]
EVENTS --> ASR[ASR 识别事件]
EVENTS --> TTS[TTS 播放事件]
EVENTS --> EXTRACT_TASK[Extract任务事件]
```

**更新** 新增post-call extractor的调试流程和pipeline_trace字段分析方法；新增sentence splitter和referee决策的专门调试指南。

**章节来源**
- [transcript/spec.md:77-127](file://openspec/specs/transcript/spec.md#L77-L127)
- [DESIGN.md:9-75](file://DESIGN.md#L9-L75)

## 结论

iSales 的"流式管道与旁路决策架构"通过以下关键设计实现了高效的实时通话处理：

1. **双LLM并行架构**：主LLM负责流式对话生成，referee负责结构化决策，显著降低首音频延迟（从6.5-9.5秒降至500-1500毫秒）

2. **旁路决策机制**：referee LLM与主LLM并行运行，在SPEAKING状态结束前提供实时决策，支持目标达成、转人工、客户拒绝等场景，具备fail-open降级保护

3. **post-call异步提取**：将客户信息抽取从实时通话流程中剥离，通过worker服务异步处理，提升实时性能，支持失败重试和状态跟踪

4. **智能切句处理**：sentence splitter模块实现流式token到句子的智能切分，优化TTS合成效率和用户体验

5. **增强的流式支持**：LLM provider新增chat_stream接口，支持供应商SSE流协议和JSON模式约束，确保输出格式一致性

6. **RoleKind枚举重构**：从{role,judge,polish}改为{main,referee,extractor}，更符合双LLM架构的设计理念

7. **松耦合的服务架构**：通过 Redis 队列和 Pub/Sub 实现服务间的解耦，支持水平扩展

8. **完善的错误处理**：多层次的降级策略和重试机制确保系统的稳定性，包括referee的fail-open机制、post-call extractor的异步处理和LLM provider的异常映射

这一架构设计为 AI 外呼销售系统提供了可靠的基础设施，支持高并发、低延迟的实时通话处理需求，同时保持了销售场景特有的目标达成和客户信息抽取能力。新增的功能模块进一步提升了系统的智能化水平和用户体验。