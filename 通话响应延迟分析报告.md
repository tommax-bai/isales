# iSales 通话响应延迟分析报告

> 范围：`isales-engine`（实时通话引擎）+ `isales-common`（Provider ABC / RTC）
> 方法：通读「用户说完话 → ASR → LLM 三层管线 → TTS → RTC 播放」完整链路，逐环节标注延迟来源。
> 日期：2026-05-30

---

## 一、说人话（先看这段）

延迟严重的**根因不是垫词、也不是网络，而是三处叠加的「串行等待」**：

1. **AI 大脑是"想完整段话才开口"**：每轮回复要先并行跑 N 个角色 LLM，再跑 N×M 个裁判 LLM，最后再跑 1 个润色 LLM —— **三层严格串行，至少 3 次大模型往返**。而且每次都是"等整段 JSON 生成完"才返回（非流式），首字延迟 = 整段生成时间。这是最大头，通常 2~5 秒。
2. **回复之前还偷偷插了一次（甚至两次）转人工判断的 LLM 调用**，串在管线前面，又加 1~2 次往返。
3. **垫词机制本身设计是对的（和管线并行），但有个 bug 级的阻塞点**：管线跑完后会 `await filler.wait_finished()` 等垫词整句播完，才开始播真正的回复 —— 垫词放音是按"整段一次性推"的，等于强行串了一段垫词时长进去。

**最划算的三个改法**：① TTS 改成边生成边播（首字延迟从"整句"降到"第一个音节"）；② 把转人工判断挪到管线之后/并行，别串在前面；③ 垫词不要 `wait_finished` 阻塞，回复音频就绪就抢播。

---

## 二、完整响应链路时序（一轮对话）

```
用户说完话
  │
  │ [P0] ASR 端点检测：靠 silence_threshold_ms 静音超时判定"说完了"
  │      └─ run_loop._await_user_or_silence() 用 asr_finals_q + 静音timeout
  ▼
ASR final 入队 asr_finals_q
  │
  │ [P1] evaluate_transfer()  ← 串行！1~2 次 LLM 调用（intent + 独立llm）
  ▼
进入 PROCESSING
  │
  ├─ filler.start()  ← 并行起垫词（设计正确）
  │
  │ [P2] run_pipeline()  ← 核心瓶颈
  │      Layer1: N 个角色 LLM  （并行，但取 max）
  │      Layer2: N×M 个裁判 LLM（并行，但串在 Layer1 之后）
  │      Layer3: 1 个润色 LLM  （串在 Layer2 之后）
  │      ⇒ 串行深度 = 3 次 LLM 往返，每次都是非流式整段
  │
  │ [P3] await filler.wait_finished()  ← 阻塞！等垫词整句播完
  ▼
SPEAKING
  │ [P4] _play_tts()  ← TTS 合成 + RTC 推流
  │      synthesize_stream 首字节延迟 + push_audio 出音
  ▼
用户听到回复
```

---

## 三、逐环节延迟点（按严重程度排序）

### 🔴 P2 —— 三层管线串行 + 全程非流式（最大瓶颈）

**位置**：`isales-engine/isales_engine/pipeline/orchestrator.py:139-228`

三层严格串行，无法重叠：

- Layer 1 角色：`_call_roles_parallel`（`orchestrator.py:231-308`）—— N 个角色 `asyncio.gather` 并行，**耗时取最慢的那个**。
- Layer 2 裁判：`_run_judges_parallel`（`orchestrator.py:311-375`）—— 必须等 Layer 1 全部返回后才启动（`orchestrator.py:170-173`），N×M 个裁判并行。
- Layer 3 润色：`_call_polish`（`orchestrator.py:377-396`）—— 必须等 Layer 2 后启动（`orchestrator.py:194`）。

**致命点：所有 LLM 调用都是非流式**。
`OpenAICompatibleLLMProvider.chat()`（`providers/llm_openai_compatible.py:62-102`）是单发 `await client.post(...)`，**等整段响应 JSON 返回**才有结果。即使只有 Layer 3，TTS 也拿不到任何 token，直到整段回复生成完毕。

> 串行深度 ≈ `max(角色) + max(裁判) + 润色`。若每次首字+生成耗时约 800ms~1.5s，单是管线就 2.4~4.5s。

**单超时设置放大尾延迟**：`pipeline_timeout_ms` 默认 8000ms（`run_loop.py:83`），并且**每一层各自用这个完整超时**（`orchestrator.py:245`、`326`、`386` 都是 `asyncio.timeout(timeout_s)`，`timeout_s = pipeline_timeout_ms/1000`）。即任何一层卡住，最坏可达 8s，三层叠加最坏 24s。这是"偶发严重延迟"的来源。

---

### 🔴 P1 —— 转人工判断串在管线之前（额外 1~2 次 LLM 往返）

**位置**：`isales-engine/isales_engine/run_loop.py:366-372` → `transfer/manager.py:59-115`

每个用户 final 在进入管线**之前**，先 `await evaluate_transfer(...)`。其中：

- `intent` 触发器（`transfer/manager.py:91-97`）：1 次 LLM 调用，超时 5s。
- `llm` 独立触发器（`transfer/manager.py:99-104`）：再 1 次 LLM 调用，超时 5s。

两者**串行**（先 intent 后 llm，`manager.py:90-104`）。若都开启，回复前白白多 2 次大模型往返。关键字/轮次触发是同步的不耗时，但 intent/llm 是网络往返，**直接叠加在用户感知延迟上**，且发生在垫词 `filler.start()` 之前（`run_loop.py:425-434` 在 transfer 之后），所以这段延迟连垫词都遮不住。

---

### 🟠 P3 —— 垫词 `wait_finished` 强制阻塞回复

**位置**：`isales-engine/isales_engine/run_loop.py:444-445`

```python
result = await run_pipeline(...)
if filler is not None:
    await filler.wait_finished()   # ← 等垫词整句播完才继续到 SPEAKING
```

设计意图（保持节奏一致）可以理解，但代价是：**只要垫词还没播完，回复就被卡住**。而垫词的播放方式本身是"整段一次性推"：

`FillerManager._stream_audio`（`filler_manager.py:181-196`）把整个 `synthesize_stream` 灌进 `telephony.audio_out`，**没有分句、没有可被回复抢占的机制**（`stop()` 只在被用户打断时调用，回复就绪时不调用）。

⇒ 实际效果：用户感知延迟 = `max(管线耗时, 垫词时长)`。如果垫词配得长（一句"好的我帮您看一下"约 1.5~2s），而管线偶尔很快，反而被垫词拖住。垫词应该是"遮蔽"延迟而非"叠加"延迟。

**附带 bug**：`_stream_audio` 返回的 `approx_duration_ms`（`filler_manager.py:193-194`）是按 mock TTS 的"每字 20ms"估算写死的，真实 Volcengine TTS 节奏对不上，这个时长只用于事件记录，不影响放音，但说明垫词时长是不可控的。

---

### 🟠 P0 —— ASR 端点检测靠静音超时（说完话到开始处理的间隔）

**位置**：`isales-engine/isales_engine/run_loop.py:558-620`，阈值 `silence_threshold_ms`

`_await_user_or_silence` 用 `asyncio.wait({asr_get, hangup_wait}, timeout=silence_s)` 等 ASR final。**关键依赖**：用户"说完了"这个判定，取决于 Volcengine ASR 何时吐出 `is_final=True` 的帧。

- 若 ASR 的 VAD/端点较慢，或 `silence_threshold_ms`（`runtime_config.py:236-241`，来自 campaign 配置）设得偏大，则"用户停止说话 → 拿到 final"之间会有一段固定空窗。
- 这段延迟在 ASR 侧，引擎只是被动等待。`asr_volcengine.py` 配置帧（`asr_volcengine.py:129-142`）里 `result_type: "full"`，未见专门的 VAD/静音参数调优。

**建议**：核对 campaign 的 `silence_threshold_ms` 实际值；并确认 ASR 厂商侧是否能更早给 final（半句确认）。

---

### 🟡 P4 —— TTS 首字节 + RTC 出音

**位置**：`providers/tts_volcengine.py:70-147`、`run_loop.py:623-664`、`transport/aliyun_rtc.py:203-231`

- TTS `synthesize_stream`（`tts_volcengine.py:94-147`）**本身是流式的**（`response.aiter_bytes()` 边收边 yield，且有 `volcengine_tts_first_byte` 首字节日志埋点），这点是好的。
- `_play_tts`（`run_loop.py:643-653`）也是 `async for chunk in tts.synthesize_stream(...)` 边拉边推，链路本身不阻塞。
- RTC `push_audio`（`aliyun_rtc.py:203-231`）有 drain 背压（buffer 满则 `await self._drain_event.wait()`），正常情况下不引入额外延迟，但 SDK buffer 行为需实测。

**结论**：TTS→RTC 这段是流式的，**不是主要瓶颈**。真正的问题是上游（P2 管线非流式）让 TTS 迟迟拿不到文本。

---

## 四、流式问题专项结论

| 环节 | 是否流式 | 说明 |
|------|---------|------|
| ASR | ✅ 流式 | WebSocket partial/final，`asr_volcengine.py` |
| LLM（管线） | ❌ **非流式** | `chat()` 等整段 JSON，`llm_openai_compatible.py:90-91` —— **核心问题** |
| TTS | ✅ 流式 | `aiter_bytes` 边收边 yield，`tts_volcengine.py:136` |
| RTC 出音 | ✅ 流式+背压 | `aliyun_rtc.py:203` |

**断点在 LLM↔TTS 之间**：因为 LLM 非流式，TTS 必须等管线完全结束、拿到完整 `result.reply` 字符串后才能开工（`run_loop.py:436-454`）。即使 TTS 自己流式，也救不了"等整段文本"这一刀。

---

## 五、优化建议（按性价比排序）

### 1.（最高优先）让回复"边生成边播" —— 打通 LLM→TTS 流式
- **难点**：现有三层管线（角色 PK + 裁判 + 润色）天然要"看到完整候选才能评判/润色"，与流式冲突。
- **务实方案 A**：仅对**润色层（Layer 3）**做 token 流式，边出 token 边喂 TTS 分句合成（句号/逗号切句）。首字延迟从"整段"降到"第一句"。
- **务实方案 B**：低延迟档位**跳过裁判+润色**，角色直接出 → 流式 TTS，仅在高价值场景启用全管线。需走 OpenSpec 改 `ai-pipeline` spec。

### 2.（高优先）转人工判断移出关键路径
- 把 `evaluate_transfer` 的 LLM 触发（intent/llm）改为**与管线并行**（`asyncio.gather(transfer, pipeline)`），或挪到回复**播放之后**异步判定。关键字/轮次触发保持同步即可。位置：`run_loop.py:366`。

### 3.（高优先）垫词不阻塞回复
- 去掉 `await filler.wait_finished()` 的硬等待（`run_loop.py:445`），改为"回复音频就绪即 `filler.stop()` 抢播"，让垫词真正只起遮蔽作用。

### 4.（中）分层超时，避免尾延迟放大
- `orchestrator.py` 三层共用 8s 全超时，应改为每层独立、更短的超时（如角色 3s / 裁判 2s / 润色 2s），并行裁判用 `return_when=FIRST` 的快速失败。

### 5.（中）核对 ASR 端点参数
- 实测 campaign `silence_threshold_ms`，确认 Volcengine ASR final 触发时机是否可前移。

---

## 六、关键代码位置索引

| 延迟点 | 文件:行 |
|--------|---------|
| 三层串行管线 | `isales-engine/isales_engine/pipeline/orchestrator.py:139-228` |
| LLM 非流式 chat | `isales-engine/isales_engine/providers/llm_openai_compatible.py:62-102` |
| 转人工串行前置 | `isales-engine/isales_engine/run_loop.py:366-372` + `transfer/manager.py:90-104` |
| 垫词阻塞等待 | `isales-engine/isales_engine/run_loop.py:444-445` |
| 垫词整段推流 | `isales-engine/isales_engine/realtime/filler_manager.py:181-196` |
| ASR 端点/静音等待 | `isales-engine/isales_engine/run_loop.py:558-620` |
| TTS 流式（正常） | `isales-engine/isales_engine/providers/tts_volcengine.py:94-147` |
| TTS 播放循环 | `isales-engine/isales_engine/run_loop.py:623-664` |
| RTC 出音背压 | `isales-engine/isales_engine/transport/aliyun_rtc.py:203-231` |
| 管线超时默认 8s | `isales-engine/isales_engine/run_loop.py:83` |
