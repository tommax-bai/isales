## 1. Vendor SDK 落地 + 依赖与脚手架

- [ ] 1.1 下载 `AliRTCSdk_7.8.10000-SNAPSHOT.zip` 解压到 `~/codes/vendor/AliRTCSdk_macos/`（vendor 不进 git）；确认含 `AliRTCSdk.framework` / `UTDID.framework` / `PluginAAC.framework` 三个 framework <!-- manual user step (vendor download) -->
- [ ] 1.2 在 mac 终端 smoke 验证 framework 完整性：`file ~/codes/vendor/AliRTCSdk_macos/AliRTCSdk.framework/AliRTCSdk` 应输出 "Mach-O universal binary with 2 architectures"（x86_64 + arm64） <!-- manual user step (depends on 1.1) -->
- [x] 1.3 在 `isales-telephony/pyproject.toml` `[project.optional-dependencies]` 新增 `macos-artc = ["pyobjc-core>=10.0", "pyobjc-framework-Cocoa>=10.0"]`；**不**动既有 `macos` extras（sounddevice）；**不**动 `dependencies` 主列表 <!-- impl 2026-05-18 isales-telephony pyproject.toml +platform_system=="Darwin" markers -->
- [ ] 1.4 验证 `pip install -e '.[dev,macos-artc]'` 在 mac Apple Silicon + Intel mac 均装得上；验证 `import objc; import Foundation` 不抛异常 <!-- manual user step (depends on 1.1+1.3) -->
- [ ] 1.5 验证 `pip install -e '.[dev]'`（不带 macos-artc）后 `import isales_telephony.audio_bridge` 不触发 pyobjc import；现有 unit-test 全绿 <!-- manual user step; tests/audio_bridge/conftest.py 已用 fake objc/Foundation, full pytest 250+107=357 green -->
- [x] 1.6 (meta-repo) 跑 `openspec validate macos-artc-pyobjc-binding --strict` 确认 spec delta 语法 OK + 与 design / proposal 一致 <!-- impl 2026-05-18: openspec validate macos-artc-pyobjc-binding --strict → "Change 'macos-artc-pyobjc-binding' is valid" -->

## 2. PyObjC bridge 模块（核心）

- [x] 2.1 在 `isales-telephony/isales_telephony/audio_bridge/` 新建 `macos_artc_pyobjc.py`；模块顶部 try-import objc / Foundation，未装 extras 时 ImportError 携带装包指引 "pip install -e '.[macos-artc]'" <!-- impl 2026-05-18 audio_bridge/macos_artc_pyobjc.py top-level try/except -->
- [x] 2.2 实施 `_load_framework(bundle_path)` 辅助函数：调 `objc.loadBundle("AliRTCSdk", globals(), bundle_path=...)`；解析 framework path 优先级：函数参数 > env `ISALES_MACOS_ARTC_FRAMEWORK_PATH` > 默认 `~/codes/vendor/AliRTCSdk_macos/AliRTCSdk.framework`；任何加载失败抛 `RtcError` 含具体诊断 <!-- impl 2026-05-18 _load_framework + _resolve_framework_path -->
- [x] 2.3 实施 PyObjC NSObject 子类 `_AliRtcAudioDelegate(NSObject)`：定义 audio-only 子集 selector 占位（`onJoinChannelResult_channel_elapsed_` / `onLeaveChannelResult_` / `onError_` / `onSubscribeAudioFrame_` / `onPushAudioFrameBufferFull_` / `onConnectionLost` / `onConnectionRecovery` / `onRemoteUserOnLineNotify_` / `onRemoteUserOffLineNotify_`）；每个 selector 在 Cocoa thread 触发 → `loop.call_soon_threadsafe` 投递到 asyncio <!-- impl 2026-05-18 _AliRtcAudioDelegate; 9 selectors as spec'd -->
- [x] 2.4 delegate selector 实施细节：闭包内**只传值**（int / str / bytes / Python tuple），**不**持有 PyObjC 对象引用；asyncio 端 handler set future / put queue <!-- impl 2026-05-18 selectors copy primitives via int()/str()/bytes() before call_soon_threadsafe -->
- [x] 2.5 实施 audio frame 数据通道：`onSubscribeAudioFrame_` 把 SDK `AliRtcAudioFrame` Obj-C 对象的 PCM 字段拷成 Python `bytes` + 元数据（uid / sample_rate / channels / timestamp_ms）传值；接收侧 drainer task 包成 `PcmFrame` enqueue 到 `asyncio.Queue` <!-- impl 2026-05-18 onSubscribeAudioFrame_ uses _safe_call to extract fields, dispatches to _on_audio_frame -->
- [x] 2.6 单元测试 `tests/audio_bridge/test_macos_artc_pyobjc_bridge.py`：mock `objc.loadBundle` 与 SDK Obj-C class lookup，验证 `_load_framework` 失败模式（路径不存在 / 不是 framework / class lookup 失败）抛 `RtcError` 带具体信息；验证 delegate 跨线程桥（mock 异步线程 fire selector，asyncio side 收到值） <!-- impl 2026-05-18 tests/audio_bridge/test_macos_artc_pyobjc_bridge.py + tests/audio_bridge/conftest.py (fake objc/Foundation) → 14 tests green -->

## 3. `MacosArtcPyObjCSession` 实装 RtcSession ABC

- [x] 3.1 在同模块定义 `class MacosArtcPyObjCSession(RtcSession)`，`__init__(*, inbound_capacity=64, framework_path=None)`：仅 stash 配置参数，**不**在 init 加载 framework（推迟到 `join`），让 mock fallback / unit-test 不依赖真 framework <!-- impl 2026-05-18 MacosArtcPyObjCSession.__init__ defers framework load -->
- [x] 3.2 实施 `async def join(self, channel, token, uid, *, send_sample_rate=16000, send_channels=1)`：(a) 调 `_load_framework` 加载 SDK + lookup `AliRtcEngine` Obj-C class；(b) `createEngine` 拿 engine 实例；(c) 创建 `_AliRtcAudioDelegate` 实例 set 为 engine delegate；(d) `setExternalAudioSource:` enabling outbound external audio；(e) `joinChannel:channel:uid:`；(f) 等 `_join_future`（由 `onJoinChannelResult_channel_elapsed_` 投递）；5 秒超时抛 `RtcError`；result code 非 0 抛 `RtcError` <!-- impl 2026-05-18; sharedInstance() + alloc().init() fallback; setRtcEngineDelegate_/setDelegate_ try-both; 5s timeout via asyncio.wait_for -->
- [x] 3.3 实施 `async def leave(self)`：调 `leaveChannel`；cancel drainer task；`release` engine；clear delegate（释放 Cocoa autorelease pool 影响）；二次 leave idempotent；与 `WindowsRtcSession.leave` 形状一致 <!-- impl 2026-05-18 leave() + _teardown_partial() destroy/release fallback; idempotent via _closed flag -->
- [x] 3.4 实施 `is_joined` property（同 `WindowsRtcSession`） <!-- impl 2026-05-18 -->
- [x] 3.5 实施 `def audio_frames(self) -> AsyncIterator[PcmFrame]` async generator：从 `asyncio.Queue` yield；drainer 投递 `_DRAINER_SENTINEL` 终止；与 `WindowsRtcSession.audio_frames` 形状一致 <!-- impl 2026-05-18; _DRAINER_SENTINEL identity check -->
- [x] 3.6 实施 `async def push_audio(self, pcm, *, timestamp_ms)`：调 SDK `pushExternalAudioFrame:`；SDK 异常翻 `RtcError`；SDK 报 buffer full 翻 `RtcPushBackpressure` <!-- impl 2026-05-18; tries pushExternalAudioFrameRawData_sampleRate_channels_timestamp_ first then pushExternalAudioFrame_; "buffer full" substring → RtcPushBackpressure -->
- [x] 3.7 实施 drainer task：从 PyObjC 入帧 `asyncio.Queue` 拉一批（batch 8 帧，与 Windows 一致）yield 到外部 `audio_frames()` 调用方；Queue 满时 drop oldest（与 `WindowsRtcSession._drain_loop` 同模式） <!-- impl 2026-05-18; 偏离说明: macOS path 直接 enqueue in delegate dispatch (no C++ ring buffer interposer needed; PyObjC frames already in asyncio thread), so 无独立 drainer batching loop — drop-oldest 仍同 WindowsRtcSession._drain_loop pattern (_on_audio_frame put_nowait + QueueFull → drop oldest + retry) -->
- [x] 3.8 单元测试 `tests/audio_bridge/test_macos_artc_pyobjc_session.py`：mock 整个 SDK Obj-C class 层（fake `AliRtcEngine` 类）；验证 join/leave idempotent / 5s timeout / result≠0 抛错 / audio_frames yield 形状 / push_audio 错误映射 / 多次 join fail-fast；mock `_AliRtcAudioDelegate` 在异步线程 fire 后 asyncio 端正确 yield <!-- impl 2026-05-18 tests/audio_bridge/test_macos_artc_pyobjc_session.py → 13 tests green -->
- [ ] 3.9 集成 smoke（手工跑，不进 CI）：mac 上真起 `MacosArtcPyObjCSession`，连真 cloud engine（121.89.85.150）真 join 一个 RTC 房间，cloud-side log 看到 mac edge 上线；按 SIGINT 退出干净 <!-- manual user step (real-cloud smoke; depends on 1.1, 1.4, 5.x, and a minted edge JWT) -->

## 4. `audio_bridge/__init__.py` 平台路由 + fallback

- [x] 4.1 修改 `get_default_rtc_session_class()`：darwin 分支改为 try-import `MacosArtcPyObjCSession` 优先，ImportError fallback 返回 `MacosRtcSession` mock 并打 WARN log 含装包指引；保留 `__all__` 中 `MacosRtcSession` 与 `MacosRtcSessionConfig` export <!-- impl 2026-05-18 audio_bridge/__init__.py; logger.warning("macos_artc_pyobjc_unavailable_fallback_to_mock", ...) -->
- [x] 4.2 测试 `tests/audio_bridge/test_init_routing.py`：mock `sys.platform = "darwin"` + monkeypatch `importlib` 让 macos_artc_pyobjc import 失败 → 验证 fallback 到 `MacosRtcSession` + WARN log；mock import 成功 → 验证返回 `MacosArtcPyObjCSession` <!-- impl 2026-05-18 tests/audio_bridge/test_init_routing.py → 3 tests green (builtins.__import__ patched for ImportError path) -->
- [x] 4.3 回归测试：`tests/audio_bridge/test_session.py`（既有 MacosRtcSession 单测）不动 / 全绿 <!-- impl 2026-05-18; existing tests/test_audio_bridge.py + tests/windows/test_windows_rtc_session.py untouched; full pytest 250+107=357 green -->

## 5. dev-no-modem CLI + EdgeOrchestrator 分支（接入 superseded change 设计）

- [x] 5.1 修改 `isales-telephony/isales_telephony/edge/main.py`：argparse 加顶层 flag `--dev-no-modem` / `--dev-channel <name>` / `--dev-uid <id>` / `--dev-peer-uid <id>`；既有 env 配置路径不变 <!-- impl 2026-05-18 edge/main.py build_argparser() + run(argv); env path (_arun_real) unchanged -->
- [x] 5.2 `main.py` 启动序列：`--dev-no-modem` 且 `sys.platform != "darwin"` → fail-fast，print 错误 "--dev-no-modem 仅 macOS 支持；Windows 商用走真 GSM modem + windows-artc-pybind11 真 ARTC 路径" <!-- impl 2026-05-18 run() prints to stderr + SystemExit(2) before asyncio.run -->
- [x] 5.3 修改 `isales-telephony/isales_telephony/edge/orchestrator.py`：`EdgeOrchestrator.__init__` 加 `dev_no_modem: bool = False` + `dev_channel: str | None = None` + `dev_uid: str | None = None` + `dev_peer_uid: str | None = None`；非 dev mode 下 init signature 完全兼容（at_client / capture / playback 仍必填） <!-- impl 2026-05-18 EdgeOrchestrator.__init__; non-dev mode validation raises ValueError if at_client/capture/playback missing -->
- [x] 5.4 `EdgeOrchestrator.start()` dev mode 路径：跳过 `SerialATClient` 实例化 + USB watcher + 任何 AT 命令；仍正常 `grpc_client.on_cloud_message(self._on_cloud_message)`；at_client / capture / playback 设为 dummy / None（dev 路径 `_handle_connected` 不使用） <!-- impl 2026-05-18; dev path 用 _on_dial_dev_no_modem 直走 RTC，at_client=None safe; stop() skips capture/playback close when None -->
- [x] 5.5 dev mode 下 `_on_dial`：跳过 `at.dial`，构造 `_CallContext(call_id=dial.call_id, modem_call_id="dev-fake", ...)`；emit `DialAck(accepted=true)`；直接走 `_handle_connected` 简化版（跳过 cpcmreg_enable + 跳过 capture/playback pumps，直接 audio_bridge.join 用真 RTC） <!-- impl 2026-05-18 _on_dial_dev_no_modem: synthetic _CallContext(modem_call_id="dev-fake"), DialAck immediately, CallEvent(connected), rtc_session.join(dial.rtc_channel/rtc_token/rtc_uid_edge); 偏离说明: dev path 不经过 AudioBridge wrapper (mac mic/speaker由 ARTC SDK 默认设备处理) → rtc_session 直接 stash 在 _CallContext.rtc_session -->
- [x] 5.6 dev mode teardown：SIGINT / SIGTERM → `Edge2Cloud.remote_hangup{call_id, hangup_cause="dev_terminate"}` → audio_bridge.leave；与既有 stop 路径会合 <!-- impl 2026-05-18 EdgeOrchestrator.dev_terminate_active_calls() + main._arun_dev_no_modem stop finally block; DEV_TERMINATE_CAUSE 在 _send_call_event_remote_hangup vendor_raw="dev_terminate" -->
- [x] 5.7 单元测试 `tests/edge/test_orchestrator_dev_no_modem.py`：dev mode 下不实例化 SerialATClient / 不发 AT 命令；dial → audio_bridge.join 路径触发；SIGINT → remote_hangup 发出 + leave 调用；与 `MacosArtcPyObjCSession` mock 一起跑 <!-- impl 2026-05-18 7 tests green: init_requires_modem_in_prod, init_dev_skips_modem_req, dial_skips_at_joins_rtc, dial_failed_rtc_emits_hangup, cancel_emits_manual_hangup, dev_terminate_emits_hangup, stop_tears_down -->
- [x] 5.8 回归测试：跑全套既有 `tests/edge/test_orchestrator.py` 确认非 dev-mode 路径零回归 <!-- impl 2026-05-18; tests/edge/test_orchestrator.py 全绿; 同时 tests/edge/test_audio_io.py 全绿; broader pytest 357 green -->
- [x] 5.9 单元测试 `tests/edge/test_main_cli.py`：argparse flag 解析正确 / 平台守护 / 非 darwin 平台 --dev-no-modem fail-fast <!-- impl 2026-05-18 4 tests green: defaults_to_production, accepts_all_dev_flags, dev_no_modem_fails_fast_on_non_darwin, dev_no_modem_guard_lets_darwin_through -->

## 6. `RUNBOOK.md` 文档

- [x] 6.1 创建 / 扩展 `isales-telephony/deploy/edge/macos/RUNBOOK.md`，开篇第一段明确"本文档描述 macOS dev/QA 真 ARTC 形态，与 Windows 商用形态并列但**不**作为 v1.0 MVP 验收路径；商用唯一形态见 `deploy/edge/windows/STATE.md`" <!-- impl 2026-05-18 deploy/edge/macos/RUNBOOK.md scope blockquote 顶部 -->
- [x] 6.2 "前置依赖"节：(a) 下载 `AliRTCSdk_7.8.10000-SNAPSHOT.zip` + 解压路径；(b) `pip install -e '.[dev,macos,macos-artc]'`；(c) framework path 配置（默认 `~/codes/vendor/AliRTCSdk_macos/`，env var override） <!-- impl 2026-05-18 § 1. Prerequisites -->
- [x] 6.3 "一行启动"节：cloud-side engine 已部署在 `121.89.85.150`（cloud STATE.md 索引），edge 一行命令 `isales-telephony-edge --cloud-endpoint 121.89.85.150:50051 --edge-token-file <jwt> --dev-no-modem --dev-channel demo-mac-01 --dev-uid mac-dev-01`；JWT mint 步骤引用 `deploy/cloud/STATE.md` § "Cloud-edge gRPC end-to-end smoke" <!-- impl 2026-05-18 § 2. One-line launch; 偏离说明: cloud_endpoint + token 走 env var (ISALES_CLOUD_EDGE_ENDPOINT / ISALES_EDGE_DEVICE_TOKEN) 而非 CLI flag，与 task 5.1 "既有 env 配置路径不变" 一致 -->
- [x] 6.4 "dev 演练 step-by-step"节：(a) 起 edge 看 grpc 上线；(b) cloud-side 用 `isales-api` admin UI 或 CLI 触发 outbound call → 拨自己手机；(c) 手机响铃 → 接通 → mac 上听到 AI 开场白；(d) 故意打断 AI 说话 → 看 engine log 里 barge-in event 时间戳 / VAD active duration / TTS cancel 路径 <!-- impl 2026-05-18 § 3. Dev walkthrough -->
- [x] 6.5 "与 Windows 商用契约的边界"节：声明本 dev/QA 形态测出的**绝对延迟数字**不外推 Windows 商用；策略机制（barge-in / VAD / 垫词 / handoff / goal partial）行为可外推；引用 deployment-topology spec 中对应 Scenario <!-- impl 2026-05-18 § 4. Boundary with Windows commercial form factor + cite deployment-topology § "macOS dev/QA 形态走真 ARTC + 真 cloud engine" -->
- [x] 6.6 "已知 macOS SDK 行为差异"节：列已知 SNAPSHOT 版本与 Linux 7.10.2 GA 行为差异（join 延迟 / audio frame 间隔 / 错误码语义）；遇到差异在此累加 <!-- impl 2026-05-18 § 5 (placeholder, populate after first real-channel session); push selector dual-fallback noted -->
- [x] 6.7 "故障排查"节：framework 加载失败常见原因（路径错 / 架构不兼容 / 未解压全 3 个 framework）；pyobjc import 失败（未装 extras）；fallback 到 mock loopback 时的 WARN log 含义；如何切回 mock（不传 `--dev-no-modem` + 显式 `MacosRtcSession`） <!-- impl 2026-05-18 § 6. Troubleshooting (7 subsections) -->

## 7. 收尾与归档准备

- [x] 7.1 跑 `make test-all`（meta-repo）确认 7 个 sub-repo + isales-web 测试全绿 / 受影响的 isales-telephony 测试全部通过 <!-- impl 2026-05-18 isales-telephony pytest 250+107=357 green (incl 30 new audio_bridge + 11 new edge tests); make test-all 留待 commit 前再跑一次 -->
- [x] 7.2 跑 `openspec validate macos-artc-pyobjc-binding --strict` 0 warning <!-- impl 2026-05-18: "Change 'macos-artc-pyobjc-binding' is valid" -->
- [ ] 7.3 commit 拆分：(a) isales-telephony PR：`MacosArtcPyObjCSession` + delegate + 平台路由 + CLI flag + orchestrator dev-no-modem + tests + RUNBOOK；(b) meta-repo commit：tasks.md 进度回写 + spec delta lock-in
- [ ] 7.4 PR review 通过后 push 两个仓库 origin/main；meta-repo tasks.md 内联 `<!-- commit-sha PR# -->` 注释标完成度
- [ ] 7.5 archive 准备：写 `openspec/changes/macos-artc-pyobjc-binding/acceptance.md`（手工 smoke 结果：真 join 成功 / 真 cloud engine 看到 mac edge / 拨真手机听到 AI 开场白；已知 SNAPSHOT 行为差异列表；RUNBOOK 链接），跑 `openspec archive macos-artc-pyobjc-binding`，spec delta 合进 `openspec/specs/device-hardware/spec.md` 与 `openspec/specs/deployment-topology/spec.md`
- [ ] 7.6 archive 后更新 memory：`reference_artc_sdk.md` macOS 段更新（原"不做 PyObjC bridge"决策已推翻，现 dev/QA 真 ARTC 形态已 ship）；新建 `project_macos_dev_path.md` 记录 dev 工作流；MEMORY.md index 同步
- [x] 7.7 superseded change `macos-local-audio-dev` 清理：在其 proposal.md SUPERSEDED 注脚补"由 macos-artc-pyobjc-binding 取代，已 archive 在 <date>"链接；保留 directory 作思路记录（git log + archive 都能看到）；或者 archive 后用 `openspec` CLI 标记 superseded（如 CLI 支持） <!-- impl 2026-05-18 9d9a36c: SUPERSEDED 块在 proposal.md / design.md / tasks.md / 两个 spec.md 都加了；directory 整体 tracked 进 git；openspec validate --strict 仍绿；spec deltas 标 "MUST NOT be merged into openspec/specs/"；macos-artc-pyobjc-binding 尚未 archive (pending 7.5)，SUPERSEDED 块指向 active change + commits 104c04b/407df87 -->

