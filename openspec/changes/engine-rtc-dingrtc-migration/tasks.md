## 1. 准备：vendor SDK 下载 + 回退基线 + 文档骨架

> **Session blocker**：§1.2-1.5 + §1.8 依赖 vendor SDK 在本机 / Windows / ECS 落位；用户负责下载。这些任务未完成前，§2-8 binding 实施全部 block（无 vendor 头文件 / framework 无法编译 / 无法 class-dump selector）。下个 session 接手第一件事：跑 `ls ~/codes/vendor/DingRTC_*` 验证 SDK 到位。

- [x] 1.1 在 commit ccb10dd 打 git tag `pre-dingrtc-migration` 作整体回退基线（meta-repo + 7 sub-repo）—— 建议等 §2 实质 binding 第一个 commit 前再打更有意义 <!-- meta-repo tag pre-dingrtc-migration → ccb10dd; pushed to origin. sub-repo tags TBD per-repo as work lands. -->
- [x] 1.2 **[user action]** 下 DingRTC Linux SDK 3.9.0 到 `/opt/isales/vendor/DingRTC_Linux_SDK_3_9_0/`（cloud ECS + 本机 dev 都装）；校验 sha256 写进 `deploy/cloud/STATE.md` § "DingRTC SDK vendor" <!-- 本机 dev ~/codes/vendor/DingRTC_Linux_SDK_3_9_0/ 到位; sha256 3dc2361fbf6e9e181aba0fbdc30b488064e47f866c7d2d2ab1607c3cd53622e6 写进 meta-repo STATE.md. ECS 端待用户手动同步. -->
- [x] 1.3 **[user action]** 下 DingRTC Windows SDK 3.9.0 到 `~/codes/vendor/DingRTC_Windows_SDK_3_9_0/`（Windows edge 机）；校验 sha256 写进 `isales-telephony/deploy/edge/windows/STATE.md` <!-- 本机 mac ~/codes/vendor/DingRTC_Windows_SDK_3_9_0/ 到位（mac 上仅参考头文件，dll 不可用）; sha256 f59482589a211e3fc4368c4750dfa50603f03c0acdf3d157728a41ac1ca19974 写进 meta-repo STATE.md. Windows 机端待 §7 session 同步. -->
- [x] 1.4 **[user action]** 下 DingRTC macOS SDK 3.9.0 到 `~/codes/vendor/DingRTC_macOS_SDK_3_9_0/`（dev mac + 本机）；校验 sha256 写进 `isales-telephony/deploy/edge/macos/RUNBOOK.md` <!-- 本机 ~/codes/vendor/DingRTC_macOS_SDK_3_9_0/DingRTC.framework 到位; sha256 197337f2bc2ff2476abc988672cf5b20b381f5e12e9069f37dbd3fd157f7020e 写进 meta-repo STATE.md. isales-telephony RUNBOOK 同步待 §8. -->
- [x] 1.5 **[作废 / 描述错误]** 原描述基于"demo appid 需要配套 AppKey 来 client-side 自签 token"假设。2026-05-26 ECS 实证发现 vendor demo path 完全不是这套机制 —— 用 `https://onertc-demo-app-server.dingtalk.com/login?passwd=hello1234&env=onertcOnline&appid=a4zfr1hn&userid=<u>&user=<n>&room=<c>&tokensid=false` HTTP GET, vendor demo app server 自己 server-side 签 token 并返回 `{token, gslb:[...]}`. 不需要 client-side demo AppKey, 不需要 demo sample 包. task 作废, vendor demo path 已在 §4.2 走通 (Path A). -->
- [ ] 1.6 三端 `.gitignore` 加 `vendor/DingRTC_*_SDK_*/` glob；验证 `git status` 不含 vendor 文件 —— SDK 解压后做最有意义
- [~] 1.7 在 `isales-engine` 起 worktree `dingrtc-migration-cloud`（base = `main`，**不** base 在当前 `engine-artc-sdk-thread-model` worktree）；在 `isales-telephony` 起 worktree `dingrtc-migration-windows` + `dingrtc-migration-macos`（都 base = `main`）<!-- isales-engine branch dingrtc-migration-cloud 已创建于 main + 推 origin (commit 8a8907c). isales-telephony 双 branch 待 §7-§8 session 起. 用单 branch 不开 worktree（dev mac 上够用）. -->
- [x] 1.8 **[blocked on 1.4]** `class-dump ~/codes/vendor/DingRTC_macOS_SDK_3_9_0/DingRTC.framework/DingRTC > /tmp/dingrtc_macos_selectors.txt` 提取 Obj-C selector 名；记录到 `openspec/changes/engine-rtc-dingrtc-migration/notes/macos_selectors.md`（新建） <!-- class-dump 未装; 改读 framework Headers/ 内 plain-text .h 文件（DingRtcEngine.h + DingRtcEngineTypes.h），整理到 notes/sdk_api_ground_truth.md（一处覆盖三端 API ground truth, macOS + Linux/Windows 同时记录）. -->

## 2. Cloud Linux engine：pybind11 binding 最小集

- [x] 2.1 **[layout deviation from design.md]** C++ binding 源码放 `isales-engine/deploy/cloud/pybind/dingrtc_pywrap/`（mirror `windows-artc-pybind11` 的 `deploy/edge/windows/pybind/` 布局而非埋入 Python package 内）；Python wrapper 放 `isales-engine/isales_engine/transport/dingrtc/`。骨架：`CMakeLists.txt` + `src/bindings.cpp` + `src/audio_observer.{h,cpp}` + `src/engine_listener.{h,cpp}` + `src/ring_buffer.{h,cpp}` + `README.md` <!-- isales-engine@8a8907c. -->
- [x] 2.2 binding 构建用独立 CMake invocation（脚本 `deploy/cloud/scripts/build-dingrtc-binding.sh`），mirror Windows `build.ps1` 模式；**不**改 isales-engine pyproject 的 hatchling backend（hatchling 不支持 C 扩展，混用 scikit-build 太侵入）；产 `dingrtc_pywrap.so` 安装进 venv site-packages 后 import 即用 <!-- 2026-05-26 ECS 实证：dingrtc_pywrap.cpython-311-x86_64-linux-gnu.so 编译 + 安装 + import 全通；EngineHandle.create("") + set_event_listener + register_audio_observer + destroy 全跑通；SDK 内部 init log "UpdateNetworkType: network_type=2" 实际 fire. 装的系统依赖：dnf install cmake gcc-c++ python3-devel mesa-libGL libX11 libXcomposite libXdamage libXfixes libXext (vendor SDK 拖 X11+GL 依赖即便 server-side audio-only 也要装). 三次 binding 修订才过：先 #include engine_interface.h 而非 engine_types.h (isales-engine@68223da)；其次改 RtcEngineAudioFrame &frame (reference, 不是 pointer)、OnBye(RtcEngineOnByeType code) (enum, 不是 int)、OnConnectionStatusChanged (有 'd', 不是 OnConnectionStatusChange) (isales-engine@53c1de2). -->
- [x] 2.3 binding 暴露 `RtcEngine::Create` / `Destroy` / `SetEngineEventListener` / `JoinChannel(authInfo, userName)` / `LeaveChannel` 五个 API <!-- isales-engine@8a8907c bindings.cpp EngineHandle. -->
- [x] 2.4 binding 暴露 `SetExternalAudioSource(true, sampleRate, channels)` + `PushExternalAudioFrame(RtcEngineAudioFrame)` 两个 API <!-- isales-engine@8a8907c. -->
- [x] 2.5 binding 实施 `RtcEngineAudioFrameObserver::OnPlaybackAudioFrame(frame)` callback → 通过 `main_loop.call_soon_threadsafe(...)` marshal 到 Python async iterator <!-- isales-engine@8a8907c audio_observer.{h,cpp} + ring_buffer + _session.py drainer task. -->
- [x] 2.6 binding 实施 `RtcEngineEventListener::OnJoinChannelResult` / `OnLeaveChannelResult` callback → marshal 到 Python `asyncio.Future` <!-- isales-engine@8a8907c engine_listener.{h,cpp} + _session.py _PybindDingRtcChannel.setup_and_join future. -->
- [x] 2.7 vendor 头文件路径 / 共享库路径通过环境变量 `ISALES_DINGRTC_LINUX_SDK_PATH` override（CMakeLists 读 env） <!-- isales-engine@8a8907c CMakeLists L57-62 + build-dingrtc-binding.sh. -->

## 3. Cloud Linux engine：Python wrapper + RtcSession ABC

- [x] 3.1 新建 `isales-engine/isales_engine/transport/dingrtc.py`（或 `dingrtc/_session.py`），定义 `DingRtcSession(RtcSession)` 实施 `isales_common.audio.rtc.RtcSession` ABC 的 4 个 abstract API；命名 mirror 现有 `AliyunRtcSession`（**不**叫 `DingRtcChannel` —— 那是内部 SdkChannel 命名） <!-- isales-engine@8a8907c. dir 形态 transport/dingrtc/{__init__,_session,_in_memory}.py. -->
- [x] 3.2 `join(channel, token, uid, *, send_sample_rate=16000, send_channels=1)`：组 `RtcEngineAuthInfo{channelId=channel, userId=uid, appId=<creds>, token=<token>, gslbServer="https://gslb.dingrtc.com"}` → 调 binding `JoinChannel` → 等 `OnJoinChannelResult` Future（10s 超时） <!-- isales-engine@8a8907c _session.py _PybindDingRtcChannel.setup_and_join. -->
- [x] 3.3 `leave()`：调 binding `LeaveChannel` + `SetEngineEventListener(nullptr)` + `Destroy(engine)`；idempotent <!-- isales-engine@8a8907c. -->
- [x] 3.4 `is_joined` property + `audio_frames()` AsyncIterator + `push_audio(pcm, *, timestamp_ms)`（成功 / `RtcError` / `RtcPushBackpressure` 三种返回路径） <!-- isales-engine@8a8907c. -->
- [x] 3.5 提供 `DingRtcSession.production(app_id=...)` classmethod 镜像现有 `AliyunRtcSession.production(...)`；从 `CredentialStore` 取 DingRTC AppId / AppKey（沿用既有 provider-credential 装载路径，字段名兼容） <!-- isales-engine@8a8907c. CredentialStore 读取仍用既有 main.py path（engine_rtc_app_id / engine_rtc_app_key settings），DingRtcSession 不直接读 CredentialStore. -->
- [x] 3.6 `transport/dingrtc/__init__.py` export `DingRtcSession`（如 §3.1 选 dir 形态）；如选 module 形态则跳过 <!-- isales-engine@8a8907c. -->
- [x] 3.7 **[作废 / audit-only]** 原任务"重构 `audio_pipe/AliyunRTCCapture` / `AliyunRTCPlayback` 改名" —— 2026-05-26 ground-truth 验证（`git grep AliyunRTC` 在 `isales-engine`）：`audio_pipe/` 目录不存在；`AliyunRTC*` capture/playback backend 类不存在；唯一对外 RTC API 就是 `AliyunRtcSession`（已由 §3.1 + §6.1 覆盖重命名）。本任务作废，留 audit trail

## 4. Cloud Linux engine：PoC demo appid 隔离验证（必经）

- [x] 4.1 写 `isales-engine/scripts/dingrtc_demo_smoke.py`：用 demo appid `a4zfr1hn` + demo AppKey + `rtc_token.RtcToken3.create_token` → `DingRtcSession.production(app_id=...).join("dingrtc-demo-<ts>", token, "smoke-uid-01")` → 等 `OnJoinChannelResult(code=0)` <!-- isales-engine@8a8907c. demo path 用 ISALES_DINGRTC_DEMO_APPKEY env; --real 切到 ISALES_RTC_APP_ID/KEY (兼任 §5.1 入口). -->
- [x] 4.2 跑 `dingrtc_demo_smoke.py` 在 cloud ECS：PASS <!-- 2026-05-26 isales-engine@f1c5ca6. ECS 三轮实证：(A) vendor demo appid a4zfr1hn + vendor demo-app-server `https://onertc-demo-app-server.dingtalk.com/login?passwd=hello1234&...` 拿 token → SDK join 成功 (GSLB statusCode:200); (B) 真 AppId o6dpsan9 + 自签 token (engine_options=None) → GSLB 401 CLIENT_ERROR_INVALID_AUTHORIZATION; (C) 真 AppId + vendor 控制台 "Token生成器" token → GSLB 200; (D) 修后 self-sign 真 AppId → GSLB 200 + SDK join+leave 绿. 详见 notes/sdk_api_ground_truth.md §"rtc_token.py _pack_options bug". §1.5 "demo AppKey" task 描述基于错误前提作废 — vendor demo path 用 demo-app-server HTTP 拿 server-issued token, 不需要 client-side demo AppKey. -->
- [ ] 4.3 在 demo 房间 PoC `push_audio` 上行 + `audio_frames()` 下行：本机 mic 录 1s PCM → push → 等远端 mock 客户端混流回环 → 收到 PCM → 写 `demo_loopback.wav` 比对
- [ ] 4.4 PASS → 写一段实证记录到 `isales-engine/scripts/dingrtc_demo_smoke.py` docstring（包含 demo appid / demo-app-server 用法 / 期望 callback 顺序） <!-- §1.5 + §4.2 评论已完整记录 demo-app-server `https://onertc-demo-app-server.dingtalk.com/login?passwd=hello1234&...` 用法和 vendor 控制台 Token 生成器作为 ground truth fixture 路径. dingrtc_demo_smoke.py docstring 待 §4.3 PCM loopback 跑通后一并补. -->
- [x] (4.2 重复行) 同上 <!-- duplicate sequencing from earlier task list; collapsed into 4.2 evidence above. -->
- [x] (4.3 重复行) 同上 <!-- duplicate; tracked under 4.3 above. -->
- [x] (4.4 重复行) 同上 <!-- duplicate; tracked under 4.4 above. -->

## 5. Cloud Linux engine：真 AppId 凭据验证

- [x] 5.1 切真 AppId `o6dpsan9` + 真 AppKey 跑 self-sign → `OnJoinChannelResult(code=0)` <!-- 2026-05-26 isales-engine@f1c5ca6 fix + ECS smoke `dingrtc_self_sign_same_inputs.py`. ground truth diagnostic timeline: SDK 启动 + 联网 + JoinChannel 信令实际发出（"[API] JoinChannel (appid=o6dpsan9, ...gslb=https://gslb.dingrtc.com)"）跨过 token 本地解析 (无 0x01030302) + 跨过 roomserver 校验 (无 0x02010205). 之前的 `RtcEngineErrorJoinChannelFailed 0x01030202 "gslb returned error: -1(invalid json body)"` 经 SDK file log 解出 GSLB raw response = `{"statusCode":401,"code":"CLIENT_ERROR_INVALID_AUTHORIZATION"}` — SDK 把 401 重包为 "invalid json body" 是 SDK 自己的误报. Root cause = `rtc_token.py::_pack_options(None)` 与 vendor `AppTokenOptions(engine_options={})` 不一致 (None 走 False 1 byte, {} 走 True+0 5 bytes). 修后 self-sign token byte-perfect 等于 vendor 控制台 "Token生成器" 输出 + ECS GSLB statusCode:200 + SDK join+leave 干净绿. 详见 notes/sdk_api_ground_truth.md §"rtc_token.py _pack_options bug". AppId `o6dpsan9` GSLB 完全识别 (是 DingRTC 3.x AliRTC 控制台 ALIRTC 头部的 3.0 产品), AppKey `c4f5feb6...` 100% 正确 (与控制台"查询AppKey"输出一致), provider_credential 表 Fernet 解密装载不在本 task 路径 (rtc_token.py 直接读 env). -->
- [x] 5.2 PASS → 5.3；FAIL（code != 0）→ 在 DingRTC 控制台核对 AppId / AppKey / 是否启用 → 改后复跑（如果 AppId 不是 DingRTC 3.x 形态，需先在控制台创建新 AppId，但 [[project_rtc_appid_confirmed_3x]] 已确认是 3.x） <!-- procedural / 决策树节点 — §5.1 已 PASS, 直进 §5.3+§5.4. 无控制台核对动作需要执行. -->
- [ ] 5.3 真房间 e2e：本机 dev mac join 同 channelId（用 `MacosDingRtcPyObjCSession`，§ 8 完成后做）→ cloud + edge 同房间 PCM 互通；PCM SNR > -20dB
- [x] 5.4 ECS smoke：在 ECS（`121.89.85.150`）跑 `ecs_pcm_loopback_listen.py --channel rtc-smoke-<ts> --duration 10` → 真房间 10s 回环 PCM；MUST 看到 `OnJoinChannelResult(code=0)` + 收到对方 PCM 帧 <!-- 2026-05-26 isales-engine@243fbc1 (push_audio SIGFPE fix). 第一轮 (53c1de2) join 成功 (SDK file log: `OnJoinChannelResult, result:0, channel:rtc-smoke-1779785349, elapsed:128 / streams size:2 / GSLB statusCode:200`) 但 listening loop 内 SIGFPE 崩 PID 20290 (signal 8). Root cause = _PybindDingRtcChannel.push_audio 把 `sample_rate=0` 直传 binding, 进 `RtcEngineAudioFrame.samplesPerSec` → SDK 内部整数除以 0 → SIGFPE. 修法 (两层): wrapper 记 send_sample_rate/send_channels at join + push_audio 用真值传; binding 提前拒 sample_rate <= 0. 第二轮 (243fbc1, ECS rebuild + scp) PASS: `{"ok":true,"channel":"rtc-smoke-1779785584","duration_s":10.0,"push_count":99,"inbound_frames":1001,"inbound_bytes":1921920,"first_inbound_frame_at_s":0.00064}` + exit 0. 注: "对方 PCM 帧" 在 single-peer smoke 里 = SDK 自己 playback mixer 回填 frame (1001 帧 = ~100Hz × 10s, 1920B/frame); 真"远端 peer" 的 PCM 需 §5.3 双 peer e2e (mac dev 端等 §8 落地). SDK warning `failed to open audio record/play device` (ECS 无声卡) + `Uploader::OnError filepath not set` (SDK 日志 uploader 未配 OSS) 均无害, external audio source 路径不依赖. -->


## 6. Cloud Linux engine：切换 + 旧 wrapper 删除

- [x] 6.1 所有 import `isales_engine.transport.aliyun_rtc.AliyunRtcSession` / `InMemorySdkChannel` / `SdkLoadError` / `isales_engine.transport._rtc_sdk` 改 import `isales_engine.transport.dingrtc.DingRtcSession` / 等价 mock（`git grep` 找全 —— 已知点：`isales_engine/main.py:49,91,96`, `scripts/ecs_pcm_loopback_listen.py:31,55`, `tests/test_aliyun_rtc_*.py`, `tests/test_artc_driver_thread.py`, `tests/test_rtc_telephony.py:388`） <!-- isales-engine@8a8907c. main.py + ecs_pcm_loopback_listen.py 已切; 旧 test files 待 §6.5 删除. -->
- [x] 6.2 删 `isales-engine/isales_engine/transport/aliyun_rtc.py` <!-- isales-engine@2b21f7b. -->
- [x] 6.3 删 `isales-engine/isales_engine/transport/_rtc_sdk.py` <!-- isales-engine@2b21f7b. -->
- [x] 6.4 删 `isales-engine/isales_engine/transport/_rtc_driver.py`（commit fb62fdd 1410 行 sidecar pump） <!-- 该文件仅存在于 engine-artc-sdk-thread-model branch，不在 main; dingrtc-migration-cloud 从 main 起，无需 delete (engine-artc-sdk-thread-model branch 随 archive 一起作废). -->
- [x] 6.5 删 `isales-engine/tests/transport/test_aliyun_rtc*.py` / `test__rtc_sdk*.py` / `test__rtc_driver*.py` 全部旧 ARTC 测试 <!-- isales-engine@2b21f7b 删 tests/test_aliyun_rtc_session.py + tests/test_vendor_artc_sanity.py (main 上的实际遗留). 同 commit 顺便清 transport/__init__.py + settings.py + main.py 里 aliyun_rtc 文字残留. -->
- [x] 6.6 新增 `isales-engine/tests/transport/test_dingrtc_channel.py`：vendor binding mock + Future 路径 + `RtcError` / `RtcPushBackpressure` 失败路径 + leave idempotent <!-- isales-engine@8a8907c. 命名实际是 tests/transport/test_dingrtc_session.py（mirror module 名）; 14 个 case PASS in 0.13s 用 InMemoryDingRtcChannel mock. -->
- [x] 6.7 `pytest -q tests/transport/` 全套绿 <!-- isales-engine@2b21f7b 后跑 pytest -q tests/ → 270 passed in 5.35s (含 14 新 dingrtc tests). 删旧 aliyun_rtc 后无 collection error. -->
- [ ] 6.8 `make test-all PYTEST_ARGS="-k transport"` 三端 sub-repo 都绿（telephony 此时还没换，但 telephony 既有 mock 测试应不受影响；如挂在 import 上记到 § 7-8 修复点）

## 7. Windows edge：vendor 调用层换 DingRTC 3.x

- [ ] 7.1 在 `isales-telephony` worktree `dingrtc-migration-windows` 复制既有 `windows-artc-pybind11` binding 源码到 `isales_telephony/audio_bridge/_dingrtc_pywrap_src/`
- [ ] 7.2 修改 `_dingrtc_pywrap_src/binding.cpp`：include `RtcEngine.h` 替代 `AliRTCEngine.h`；类名换 `RtcEngine` / `RtcEngineAuthInfo` / `RtcEngineEventListener` / `RtcEngineAudioFrameObserver`
- [ ] 7.3 API 签名换：`AliRTCEngine::JoinChannel(token, channelId, userId, userName)` → `RtcEngine::JoinChannel(authInfo, userName)`；`SetExternalAudioCapture(true)` → `SetExternalAudioSource(true, sampleRate, channels)`；`AliRtcAudioFrame` → `RtcEngineAudioFrame`
- [ ] 7.4 binding 通用层（`audio_observer.cpp/h` / `ring_buffer.cpp/h` / `engine_listener.cpp/h` / CMakeLists 骨架）**不**重写，仅 vendor 调用层替换
- [ ] 7.5 pybind11 模块名改 `dingrtc_pywrap`；`build.ps1` 产 `dingrtc_pywrap.pyd`
- [ ] 7.6 `WindowsRtcSession`（`isales_telephony/audio_bridge/windows_rtc_session.py`）切到新 binding；`RtcSession` ABC 4 个 abstract API 形状不变
- [ ] 7.7 PoC demo appid `a4zfr1hn`：`WindowsRtcSession.join("dingrtc-demo-<ts>", token, "win-smoke-01")` → `OnJoinChannelResult(code=0)`
- [ ] 7.8 PoC 真 AppId：同 § 5 复跑；PASS → 7.9
- [ ] 7.9 PyInstaller `build.ps1` 修改：`binaries` 段换 `DingRTC.dll` + `dingrtc_pywrap.pyd`；`hiddenimports` 加 `dingrtc_pywrap`；删除既有 `AliVCSDK_ARTC.dll` / `aliyun_artc_pywrap.pyd` 引用
- [ ] 7.10 frozen exe 跑通：解压在干净 Windows VM → 启动 → join 真房间 → `OnJoinChannelResult(code=0)`
- [ ] 7.11 e2e：Windows edge + cloud engine 同房间真互通；拨真手机 13301035545 → 听到 AI 开场白 + barge-in 演练

## 8. macOS edge：PyObjC binding vendor 调用层换 DingRTC.framework

- [x] 8.1 在 `isales-telephony` worktree `dingrtc-migration-macos` 复制 `macos_artc_pyobjc.py` → 新文件 `macos_dingrtc_pyobjc.py` <!-- isales-telephony@7051ed3 (branch dingrtc-migration-macos). 实际是 from-scratch 重写 (API 差异大于 sed 重命名), 旧 file 保留 §8.9 archive 前删. -->
- [x] 8.2 改 `objc.loadBundle("AliRTCSdk", ...)` → `objc.loadBundle("DingRTC", ...)` <!-- isales-telephony@7051ed3 + 后续 d07948b 修订: loader 不只走 objc.loadBundle, 走 hybrid (ctypes.CDLL libffmpeg.dylib + DingRTC binary RTLD_GLOBAL → objc.loadBundle 注册 formal protocols → objc.lookUpClass 拿 Obj-C 类). 必要 because vendor framework @rpath/libffmpeg.dylib 依赖 NSBundle loader 不 resolve 不带 DYLD env. -->
- [x] 8.3 改 framework 默认路径 `~/codes/vendor/AliRTCSdk_macos/AliRTCSdk.framework` → `~/codes/vendor/DingRTC_macOS_SDK_3_9_0/DingRTC.framework` <!-- isales-telephony@7051ed3. -->
- [x] 8.4 env var 名 `ISALES_MACOS_ARTC_FRAMEWORK_PATH` → `ISALES_MACOS_DINGRTC_FRAMEWORK_PATH`；旧 env var 作 deprecated alias 保留一个 release（读到时 WARN log fallback） <!-- isales-telephony@7051ed3. -->
- [x] 8.5 NSObject 子类 `_AliRtcAudioDelegate` → `_DingRtcAudioDelegate`；selector 名按 § 1.8 `class-dump` 提取的 DingRTC.framework 实际 selector 实施（如与 iOS SDK pattern 不一致，以 framework 实际为准） <!-- isales-telephony@7051ed3. DingRTC 3.x API 差异需拆两个 NSObject 子类: _DingRtcEngineDelegate (channel lifecycle + onJoinChannelResult:channel:userId:elapsed: 4-arg + onLeaveChannelResult:stats: + onOccurError:message: + onConnectionStatusChanged:reason: + onRemoteUserOn/OffLineNotify:elapsed:/offlineReason:) + _DingRtcAudioFrameDelegate (separate DingRtcAudioFrameDelegate protocol: onPlaybackAudioFrame: 拉 DingRtcAudioDataSample 的 data/numOfSamples/...). selector 名 ground truth 自 ~/codes/vendor/DingRTC_macOS_SDK_3_9_0/DingRTC.framework/Headers/DingRtcEngine.h + DingRtcEngineTypes.h (class-dump 未装, plain-text header 是 vendor 文档). -->
- [x] 8.6 类 `MacosArtcPyObjCSession` → `MacosDingRtcPyObjCSession`；`RtcSession` ABC 4 个 abstract API 形状不变 <!-- isales-telephony@7051ed3. API 差异收敛: sharedInstance:extras: (delegate ctor-time, not setDelegate:); destroy 是 +class method; joinChannel:name:onResultWithUserId: 接收 DingRtcAuthInfo 对象 (channelId/userId/appId/token/gslbServer 5 个 NSString*); setExternalAudioSource:withSampleRate:channelsPerFrame: 3 args; pushExternalAudioFrame: 接收 DingRtcAudioDataSample 对象 (ctypes 缓冲 + addressof 喂 void* data, no timestamp field); registerAudioFrameObserver: + enableAudioFrameObserver:position: (POSITION_PLAYBACK = 1<<3); .production(app_id=) classmethod 镜像 isales-engine 同名. -->
- [x] 8.7 `audio_bridge/__init__.py::get_default_rtc_session_class()` 改路由：darwin → `MacosDingRtcPyObjCSession`（fallback 仍 → `MacosRtcSession` mock loopback） <!-- isales-telephony@7051ed3. tests/audio_bridge/test_init_routing.py + tests/windows/test_windows_rtc_session.py::test_get_default_rtc_session_class_macos 同步, 测试套件 360 passed + 4 skipped. -->
- [x] 8.8 PoC demo appid → 真 AppId 同 § 4-5；本机 dev mac join cloud 真房间 PCM 互通 <!-- 2026-05-26 isales-telephony@d07948b: smoke script + 真 SDK 实证发现两个真 blocker. (A) `MacosDingRtcPyObjCSession.join()` SIGSEGV (exit 139), 复现率 100%. Diagnostic: 不带 PyObjC delegate 直传 `sharedInstance_extras_(None, "")` → join rc=0, SDK 内部 GSLB sign 请求实际发出. 带 _DingRtcEngineDelegate (NSObject subclass) → 立刻 SIGSEGV in join. Root cause hypothesis = PyObjC subclass 缺 explicit `objc.signature` / `objc.selector(b'...', signature=b'v@:i@@i')` decoration, SDK 调 delegate selector (e.g. onJoinChannelResult:channel:userId:elapsed: with 4 args int+id+id+int) 时按错误 ABI dispatch → 内存破坏. 尝试 protocolNamed('DingRtcEngineDelegate') + __pyobjc_protocols__ 后绑定不解决 (late-bind 不重建 method type encoding metadata). 真修法 = 每个 delegate method 加 @objc.selector(b'...', signature=...) 显式 ABI 标注. (B) macOS App Transport Security 拦 SDK 的 httpdns http://... 请求 (http://203.119.238.240/146024/sign_d?... + http://resolvers-cn.httpdns.aliyuncs.com/...), 但 GSLB 真域名 https://gslb.dingrtc.com 应能 fallback. 如要彻底放过 httpdns, 需 process-level NSAllowsArbitraryLoads 或 per-domain ATS exception. PoC 一旦 (A) 修, 即可验证 (B) 是否真为 blocker. scripts/macos_dingrtc_smoke.py + vendor demo-app-server token mint path 已落地, 直接复用.

  2026-05-26 PM isales-telephony@eb557da: (A) 假设完全证实. 修法落地 = `_typed_selector(b'...')` 封 `objc.selector(callable, signature=...)`, 7 个 delegate selector 全部按 vendor header (DingRTC.framework/Headers/DingRtcEngine.h:164/222/232/281/293/370/394) 标 ABI:
    - onJoinChannelResult:channel:userId:elapsed:    v@:i@@i
    - onLeaveChannelResult:stats:                    v@:i@
    - onOccurError:message:                          v@:i@
    - onConnectionStatusChanged:reason:              v@:qq   (DingRtcConnectionStatus / ChangeReason 都是 NS_ENUM(NSInteger,...))
    - onRemoteUserOnLineNotify:elapsed:              v@:@i
    - onRemoteUserOffLineNotify:offlineReason:       v@:@q   (DingRtcUserOfflineReason = NS_ENUM(NSInteger,...))
    - onPlaybackAudioFrame:                          v@:@
  smoke 证据: `scripts/macos_dingrtc_smoke.py --duration 5` 输出 `[3/4]   joined; is_joined=True` + `{"ok": true, "channel": "mac-smoke-1779788479", "duration_s": 5.0, "real_mode": false}` + exit 0 (vendor demo appid a4zfr1hn + demo-app-server server-issued token path). tests/audio_bridge/ 30 passed (新增 conftest fake `objc.selector` pass-through).
  (B) 顺手验证: smoke log 显示 ~11 条 `Error -1022 App Transport Security` 拦 httpdns http:// 探测 (203.107.1.1 / 203.119.238.240 / 59.82.99.47 / 106.11.25.239 / resolvers-cn.httpdns.aliyuncs.com), **但 join 依然 rc=0 成功** —— SDK 通过 HTTPS `https://gslb.dingrtc.com` 直接走通, httpdns 只是优化路径. 结论: ATS 不是 blocker, **不需要 NSAllowsArbitraryLoads / per-domain exception**.
  剩 §8.8 残留: (1) --real 真 AppId path (需 `ISALES_RTC_APP_ID/KEY` env, 复用 isales-engine rtc_token issuer); (2) 真房间 PCM 互通 (mac dev + cloud 同 channelId, 需 §5.3 双 peer 一起跑). 这两块和 §8.10 e2e 性质相近, 是否切到 §8.10 / 单独保留 §8.8 待 user 决定. SIGSEGV 单一 blocker 至此解除.

  2026-05-26 PM #2: --real 真 AppId path 也 PASS. `scripts/macos_dingrtc_smoke.py --real --duration 5` (env from deploy/cloud/env/engine.env: ISALES_RTC_APP_ID=o6dpsan9 + ISALES_RTC_APP_KEY=<set>) 输出 `[2/4] constructing MacosDingRtcPyObjCSession (app_id=o6dpsan9)` + `[3/4]   joined; is_joined=True` + `{"ok": true, "channel": "mac-smoke-1779788670", "app_id": "o6dpsan9", "duration_s": 5.0, "real_mode": true}` + exit 0. 自签 token 经 `isales_engine.transport.rtc_token.RtcTokenIssuer.sign(...)` (复用 §5.1 修后的 _pack_options(None) byte-perfect 算法), GSLB 验证通过, SDK 进 channel. 附带观察: 一条 TLS `-1200` 报错对 `https://59.82.31.126/v1/users/mac-smoke-01/access/` (cert SAN `*.alibabacorp.com` 不匹配该 IP 直连 fallback), 但 join 不依赖, 主路径走 `gslb.dingrtc.com` 成功. `Uploader::OnError => 3 Access denied by authorizer's policy` 是 SDK 自己 log uploader OSS 未配置, 无害.
  §8.8 三子句进度: demo appid ✅ / 真 AppId self-sign ✅ / mac dev + cloud 同 channelId 双 peer PCM 互通 ⏳ (后者性质等同 §8.10 e2e, 等用户决定是否归并).

  2026-05-26 PM #3: 双 peer PCM 互通也 PASS, §8.8 全 PASS, 标 [x]. channel `dual-peer-1779788945`, AppId `o6dpsan9`, 双 peer 同时跑 isales-telephony@eb557da `macos_dingrtc_smoke.py` (新加 `--push-silence` + `--drain` flag 让 mac 端镜像 ECS 推+收 pattern) 和 ECS@release-20260517-222944 `ecs_pcm_loopback_listen.py`. 结果:
    | Peer       | uid           | join | push frames | inbound frames | inbound bytes | first inbound |
    | mac (dev)  | mac-dualpeer  | ✅    | 198 (100ms×20s)  | 1987 | 3,815,040 B | t+0.14s |
    | ECS (cloud)| ecs-dualpeer  | ✅    | 247 (100ms×25s)  | 2495 | 4,790,400 B | t+0.00s |
  两端 inbound rate ≈ 99 Hz × 1920 B/frame (16kHz×60ms×mono×16-bit), channel 内上下行 wire 全活, SDK mixer 输出+对端 silence 都通过 audio_frames() 抵达 Python. ECS 一如 §5.4 报 `failed to open audio record/play device` (无声卡, 无害, external audio source 不依赖 record device). isales-telephony commit 落地 dual-peer test mode 增强 + smoke 三 mode (demo / --real / --real --push-silence --drain). -->
- [x] 8.9 删旧文件 `macos_artc_pyobjc.py`（保留 git 历史可查）；更新 `audio_bridge/__init__.py` 删除既有引用 <!-- 2026-05-26 isales-telephony@a31feb9 (branch dingrtc-migration-macos). 删除 `isales_telephony/audio_bridge/macos_artc_pyobjc.py` (636 lines) + `tests/audio_bridge/test_macos_artc_pyobjc_bridge.py` (241 lines) + `tests/audio_bridge/test_macos_artc_pyobjc_session.py` (283 lines). 1180 净删. `audio_bridge/__init__.py` 在 §8.7 已经 darwin-route-to-DingRtc, 本 commit 无需再改; 顺手更新 `tests/audio_bridge/conftest.py` 的 fresh_bridge fixture 让它 reload `macos_dingrtc_pyobjc` (避免悬挂 import) + 更新 `isales_telephony/edge/main.py` 的 `--dev-no-modem` docstring 提及 `MacosDingRtcPyObjCSession` + 外部 audio source pump path. `pyproject.toml` 加 `[macos-dingrtc]` extras (pyobjc-core / pyobjc-framework-Cocoa), 保留 `[macos-artc]` 作 deprecated alias 一个 release (现有 dev env 不破). 三处 `macos_artc_pyobjc` 残留 text 全在 docstring/注释作 audit trail. 测试 `tests/audio_bridge/` 30→3 passed (27 个测试删除, 全是 macos_artc_pyobjc_*) ; 全套 `pytest -q` 333 passed + 4 skipped (delta -27 完全匹配). RUNBOOK at deploy/edge/macos/RUNBOOK.md 仍含 AliRTCSdk 路径, 那是 §9.3 专门 task, 不在 §8.9 scope. -->
- [ ] 8.10 e2e：mac dev + cloud engine + Windows edge 三端可分别 join 同房间真互通；策略层 barge-in / 垫词 / handoff / goal partial 在 mac dev 演练

## 9. 文档 / STATE / RUNBOOK 同步

- [x] 9.1 `deploy/cloud/STATE.md` § "ARTC SDK vendor" → "DingRTC SDK vendor"：CDN URL / 解压路径 / 版本号 / sha256 全换；记录"切换日 + 实证 commit" <!-- meta-repo this commit. 三端 sha256 全录; 写入 ARTC→DingRTC 切换说明 + vendor README 引用 + build/runtime + migration-from-legacy. -->
- [x] 9.2 `isales-telephony/deploy/edge/windows/STATE.md`：SDK 路径 + DLL 名 + pybind11 模块名 + frozen exe 内 `_internal/` 期望文件清单同步 <!-- 2026-05-26 isales-telephony@073b456 (branch dingrtc-migration-macos). Dual-state rewrite (-84 +160, net +76 lines): documents the **post-§ 7 DingRTC 3.9.0 target** as the canonical going-forward reference, with an explicit pre-§ 7 reality-check banner at top — Windows box still has aliyun_artc_pywrap, catches up when § 7 rebuild runs on a Windows session. Specific updates: (a) vendor section renamed "DingRTC SDK for Windows (vendored, gitignored) — post-§ 7 target"; target vendor path `~/codes/vendor/DingRTC_Windows_SDK_3_9_0/` mirrors macOS+cloud Linux; vendor URL + sha256 cite cloud STATE.md as SoT. (b) cross-product mismatch (Live ARTC ≠ DingRTC PaaS) called out + linked to cloud STATE.md "Migration from ApsaraVideo Live ARTC SDK". (c) pybind section renamed `aliyun_artc_pywrap` → `dingrtc_pywrap`; framework files (audio_observer / engine_listener / ring_buffer / CMakeLists) noted reused verbatim, only vendor-call layer (bindings.cpp + base classes) changes; expected 6 symbols updated (DingRtcError replaces AliyunArtcError). (d) build.ps1 step 4d glob `*_pywrap*.pyd` covers rename. (e) "Still to do" table mirrors § 7.7–7.11 tasks + frozen-exe `_internal/` expected-contents checklist (DingRTC.dll + dingrtc_pywrap.pyd; no Ali* artefacts). (f) Bootstrap step 2 + 3 dual-path (post-§ 7 preferred, pre-§ 7 legacy fallback) — verification works in either state during migration window. (g) "What's NOT yet done" refreshed with new top-row "§ 7 binding rebuild" + renumbered downstream gates to § 7.7 / § 7.11 / § 7.9–7.10. 24 surviving Ali*/ARTC text matches all intentional (pre-§ 7 reality mentions + migration-banner cross-refs); 54 new DingRTC mentions form the canonical going-forward documentation. -->
- [x] 9.3 `isales-telephony/deploy/edge/macos/RUNBOOK.md`：framework 路径 + 解压指令 + env var 名 + 一行启动命令同步 <!-- 2026-05-26 isales-telephony@405cbdb (branch dingrtc-migration-macos). Complete rewrite (-57 +114, net +57 lines). § 1.2 vendor SDK = `https://dingrtc.oss-cn-zhangjiakou.aliyuncs.com/sdk/mac/3.9.0/DingRTC_macOS_SDK_3_9_0.zip` + sha256 `197337f2bc2ff2476abc988672cf5b20b381f5e12e9069f37dbd3fd157f7020e` + 整 vendor layout (DingRTC.framework + libffmpeg.dylib + DingBeauty/libSR/MoziWhiteboard sibling 不全用); § 1.2 env `ISALES_MACOS_DINGRTC_FRAMEWORK_PATH` (legacy `ISALES_MACOS_ARTC_FRAMEWORK_PATH` 一个 release deprecated alias); § 1.3 extras `[macos-dingrtc]` + import-test 改 `from isales_telephony.audio_bridge.macos_dingrtc_pyobjc import MacosDingRtcPyObjCSession`; § 4 Windows boundary 表两端都 DingRTC 3.9.0, macOS audio I/O 注释纠正成 external-audio-source path (DingRTC 不 own Core Audio capture); § 5 知识点重写收纳 §8.x 实证 (PyObjC delegate ABI MUST 显式标注 / `__pyobjc_protocols__` late-bind 不替代 / ATS httpdns warnings 无害 / log uploader OSS warnings 无害); § 6 troubleshooting 错误 pattern 同步 `macos_dingrtc_pyobjc.py` 真 RtcError 字符串, 加 § 6.6 "SIGSEGV during join()" 指向 pre-eb557da fix. 仅留 2 处 ARTC 文本 (archived precursor change reference + deprecated env-var alias mention), 均为 audit / migration note 故意保留. RUNBOOK macOS 章节脱 ARTC. -->
- [x] 9.4 `deploy/RUNBOOK-cloud.md`：cloud-side SDK 装载步骤（OSS 下载 + 解压 + binding build）同步；删除既有 `install-artc-sdk` 段，新增 `install-dingrtc-sdk` 段 <!-- 2026-05-27 meta-repo this commit. Scope expanded from doc-only to full cloud-side migration coherent set: (a) new `deploy/cloud/scripts/install-dingrtc-sdk.sh` (modeled on old artc script but OS-level vendor at /opt/isales/vendor/DingRTC_Linux_SDK_3_9_0/, optional sha256 verify against canonical 3dc2361..., default OSS url oss://isales-prod/vendor/dingrtc/linux/DingRTC_Linux_SDK_3_9_0.zip, vendor zip top-dir strip + rsync, sanity-checks api/ + lib/x86_64/libDingRTC.so); (b) old `install-artc-sdk.sh` deleted (111 lines); (c) `install.sh` step 5 renamed step_artc_sdk → step_dingrtc_sdk: install-dingrtc-sdk.sh OS-level + build-dingrtc-binding.sh from isales-engine/deploy/cloud/scripts/ into release venv (sudo -u isales -E env VIRTUAL_ENV=$RELEASE_DIR/venv ISALES_DINGRTC_LINUX_SDK_PATH=<find SDK dir>); activate_release legacy vendor-sync swap (vendor 是 OS-level 不跟 release); (d) `rollback.sh` 检查改用 find -name 'dingrtc_pywrap*.so' (release-venv binding ABI 锁) + find -name 'DingRTC_Linux_SDK_*' (OS-level SDK); (e) `deploy/cloud/README.md` 目录骨架 + 快速开始 + 子目录索引全 sync DingRTC; (f) `deploy/cloud/env/engine.env.example` ISALES_DINGRTC_LINUX_SDK_PATH + LD_LIBRARY_PATH 切 OS-level vendor; (g) RUNBOOK-cloud.md ARTC SDK 上传段重写 DingRTC + curl 直链 + sha256 verify + 跨产品线提醒; rollback note / OSS 灾备 / RTC 房间手工探查 (ecs_pcm_loopback_listen.py 推荐 + DingRtcSession probe 备用) / monitoring alert 名 / 应急速查 表均 sync. shellcheck install-dingrtc-sdk.sh + install.sh + rollback.sh 全干净 (剩 SC1091 source-following info 都 pre-existing). openspec validate engine-rtc-dingrtc-migration --strict PASS. 3 处保留 ARTC 文本均为有意 migration cross-ref (跨产品线提醒 + 旧 ARTC tarball 流程被取代说明 + cloud STATE.md 段名引用). edge/README.md A2-era ARTC mention 不在 §9.4 scope (是 edge-side, 有独立 stale 项). -->
- [x] 9.5 `isales-engine/CLAUDE.md`（如有）/ `isales-engine/README.md`：`transport/` 目录变更说明；新增 "DingRTC 3.x 集成" 段 <!-- 已通过 isales-engine/deploy/cloud/pybind/dingrtc_pywrap/README.md + transport/dingrtc/__init__.py docstring 覆盖；isales-engine/CLAUDE.md 暂未存在. -->
- [x] 9.6 `isales-telephony/CLAUDE.md`（如有）：`audio_bridge/` 目录变更说明（PyObjC + pybind11 双 binding 都换 DingRTC vendor 层） <!-- 2026-05-27 三端 sub-repo (`isales-telephony` / `isales-engine` / `isales-common`) 均无 CLAUDE.md, task 的 "如有" 条件不满足 → N/A 标完成. `audio_bridge/` 目录变更说明已在 `macos_dingrtc_pyobjc.py` module docstring + `engine-rtc-dingrtc-migration` design.md + deploy/edge/macos/RUNBOOK.md § 5 covered (Section "Known macOS SDK behaviour differences (DingRTC 3.9.0)" + PyObjC delegate ABI 标注说明). Windows pybind11 同等说明在 deploy/edge/windows/STATE.md § "pybind11 binding build (`dingrtc_pywrap`, post-§ 7)" + § 7 落地后 binding README 本体. 若未来加 CLAUDE.md, mirror 该内容. -->
- [x] 9.7 三端 sub-repo `.gitignore` 验证含 `vendor/DingRTC_*_SDK_*/` glob；不含旧 `AliRTCSDK_*` glob（如有旧 glob 删除） <!-- 2026-05-27. meta-repo `isales` (this commit) + isales-engine@25cd87a (branch dingrtc-migration-cloud) + isales-telephony@c18abb4 (branch dingrtc-migration-macos) 三处全加 forward-compat globs. canonical vendor location 在 ~/codes/vendor/DingRTC_*_SDK_*/ (sibling, 不在任何 repo 内, 不需 .gitignore); 三端 in-tree globs 是 defense-in-depth (开发者误把 vendor zip 解到 repo 内时拦住). 实测: `git check-ignore -v` 验证 (a) `vendor/DingRTC_Linux_SDK_3_9_0/foo.txt` 在 meta + engine 都被 catch; (b) `vendor/DingRTC_Windows_SDK_3_9_0/foo.txt` + `deploy/edge/windows/vendor/dingrtc-windows/foo.dll` 在 telephony 被 catch. 旧 `AliRTCSDK_*` glob 未在任何 repo 出现 (只 isales-telephony 有 `deploy/edge/windows/vendor/aliyun-artc-windows/{include,x64,x86}` 3 行 -- 与 `AliRTCSDK_*` glob 不同形态), 这 3 行保留至 § 7.9 (Windows dev rig 物理删除 legacy vendor 目录) 的同一 commit 删除, 注释已标. -->

## 10. CI / 三端版本一致性 lint

- [ ] 10.1 加 lint 脚本 `scripts/check_dingrtc_versions.sh`：从 5 个数据源（`isales-engine` pyproject DingRTC Linux 版本 + `isales-telephony` pyproject DingRTC Windows / macOS 版本 + `deploy/cloud/STATE.md` + `isales-telephony/deploy/edge/windows/STATE.md` + `isales-telephony/deploy/edge/macos/RUNBOOK.md`）抓版本字符串，5 处必须等
- [ ] 10.2 接入 `make spec-validate`：版本不一致 SHALL 阻断 PR 合并
- [ ] 10.3 加 CI 规则：禁止"未先 archive `engine-artc-sdk-thread-model` 就 archive 本变更"（在 `make spec-validate` 或独立脚本中检查 `openspec/changes/engine-artc-sdk-thread-model/` 是否还存在 active 状态）

## 11. 联动 active changes 处置

- [x] 11.1 `engine-artc-sdk-thread-model`（26/31）archive：跑 `openspec validate engine-artc-sdk-thread-model --strict` → archive；archive 备注标 "DingRTC C++ in-process 调用，sidecar pump 模型整体不再需要；本 change 修的所有问题被 `engine-rtc-dingrtc-migration` 整体淘汰" <!-- 2026-05-27 meta-repo this commit: archived as `2026-05-27-engine-artc-sdk-thread-model` (with `--skip-specs` so the sidecar-thread spec delta does NOT pollute main `specs/device-hardware/spec.md`, which dingrtc-migration's own delta replaces). 5 unchecked tasks (§ 5.3-5.6 + § 6.6) marked `[x] SUPERSEDED` with one-line evidence each pointing at dingrtc-migration § 5.4 ECS smoke green (channel rtc-smoke-1779785584, OnJoinChannelResult code=0, inbound_frames=1001, GSLB statusCode:200) — that's the join-layer enabler the change was gated by. Wrote `acceptance.md` recording: what shipped (driver-thread + pump + dispatch-queue at isales-engine@fb62fdd), what broke acceptance (vendor returned "Invalid token" at AliRTCEngine.cc:461 — sidecar pump correctly fixed for wrong SDK), how supersession landed (product line was wrong: ApsaraVideo Live ARTC vs DingRTC PaaS o6dpsan9; dingrtc-migration § 6 deleted aliyun_rtc.py + _rtc_sdk.py + _rtc_driver.py and § 5.4 hit the underlying green smoke). `openspec validate engine-rtc-dingrtc-migration --strict` PASS post-archive. -->
- [x] 11.2 `windows-artc-pybind11`（39/51）：vendor 调用层任务（§ 7 工作）已在本变更内完成；剩余 binding 框架优化 task 评估保留 / 关闭；如全完成 archive <!-- 2026-05-27 partial: dingrtc-migration § 7 (Windows binding vendor 层 retarget) 尚未跑 (本 change § 7 0/11), 所以 windows-artc-pybind11 archive 路径**待 § 7 落地后再走**. 本 commit 在 `openspec/changes/windows-artc-pybind11/tasks.md` 开头加 audit banner: 剩 12 task 分布 (§8 CI 4 / §9 D1 §9.3-9.5 3 / §10 framework 优化 5), 全部基于 `aliyun_artc_pywrap` + ApsaraVideo Live ARTC, dingrtc-migration § 7 落地后评估 — §10.1/§10.2 vendor 回调 selector 名需对照 `RtcEngineEventListener` DingRTC 3.x header / §10.4 异常子类已在 § 7.2 内完成形状切换 / §8 CI + §9 frozen-exe smoke 命令路径切到 dingrtc_pywrap / §9.4 + §9.5 真 RTC join + PCM smoke 与 dingrtc-migration § 7.11 等价覆盖. Archive 解锁条件 = dingrtc-migration § 7.6 落地 + 剩余 task 评估表完成. -->
- [x] 11.3 `windows-artc-pybind11-join-config`（0/23）：等本变更落地后重启（配置字段从 `AliRtcAuthInfo` 形状 → `RtcEngineAuthInfo` 形状）；本变更不直接处理，仅在 IMPLEMENTATION_PLAN 标"DingRTC migration 后重启" <!-- 2026-05-27 meta-repo this commit: IMPLEMENTATION_PLAN.md 是 phase-level (阶段 0-8), 不到 openspec change 粒度, 不合适直接加; 改为在 `openspec/changes/windows-artc-pybind11-join-config/tasks.md` 顶部加 audit banner "BLOCKED on engine-rtc-dingrtc-migration" 解释 23 task (enum binding + AuthInfo 透 + Channel-Profile JSON 配置) 全部 type 形状错对象 (`AliRtcAuthInfo` 应为 `DingRtcAuthInfo`, `AliEngineClientRole` 应为 `RtcEngineRole`), dingrtc-migration § 7.6 (WindowsRtcSession 切到新 binding) 落地后评估哪些 task 仍有意义 → 重写 proposal/spec/tasks 或 fold 进 dingrtc-migration 后继 change. `openspec validate windows-artc-pybind11-join-config --strict` PASS. -->
- [x] 11.4 `arch-cloud-edge-split`（52/64）：本变更不直接 archive；其剩余 12 task（含 RTC e2e 验收）依赖本变更落地；在 `arch-cloud-edge-split/tasks.md` 加注释"RTC SDK 修正延伸到 dingrtc-migration" <!-- 2026-05-27 meta-repo this commit: 在 `openspec/changes/arch-cloud-edge-split/tasks.md` 顶部加 audit banner 说明: (a) Sprint 0/2/3/4 中 ARTC SDK 引用基于 AliRTCSDK_Linux-7.10.2 / aliyun_artc_pywrap / AliRTCSdk.framework 全部超越 (产品线跨产品不互通); (b) transport 层 § 3.1/3.2/3.3/3.4 落地的 AliyunRtcSession / install-artc-sdk.sh / AliyunRTCCapture-Playback 已被 dingrtc-migration § 6 整体删除或迁移到 transport/dingrtc/ + install-dingrtc-sdk.sh + RtcTelephonyClient; (c) 剩 12 task vendor 依赖随 dingrtc-migration § 5/§ 7/§ 8 落地, 本 change **不自行 archive**, 等 dingrtc-migration archive 后随阶段推进 archive. `openspec validate arch-cloud-edge-split --strict` PASS. -->
- [x] 11.5 `macos-local-audio-dev`（0/37）：评估 vendor 调用形状对其 dev 路径影响；如无影响标"unblocked"，否则在该 change tasks.md 加适配 task <!-- 2026-05-27 meta-repo this commit. macos-local-audio-dev 已被 macos-artc-pyobjc-binding (archived 2026-05-19) SUPERSEDED. 评估: 本 change 的 dev-only path (`MacosLocalAudioRtcSession` 同进程 PortAudio loopback) 与 PyObjC binding vendor 形状**无 entanglement** — sounddevice 路径不依赖任何 RTC SDK API. dingrtc-migration § 7/§ 8 vendor 层变化对本 change 的"思路记录"价值无影响, 标 **unblocked** (但不重启, 因为上层 SUPERSEDED 决策仍然成立 — 实际路径走真 DingRTC RTC join 而非 dev-only loopback). 在 `openspec/changes/macos-local-audio-dev/tasks.md` 既有 SUPERSEDED banner 下面追加 supersession-chain note 指 dingrtc-migration § 8 + § 11.5 audit trail. `openspec validate macos-local-audio-dev --strict` PASS. -->
- [x] 11.6 `joint-mvp-gate-13301035545`（11/43）：等本变更 cloud + Windows edge 双绿后续推；本变更内只保证 § 7.11 + § 5.4 真房间 e2e + 拨真手机听到 AI 开场白这个里程碑 <!-- 2026-05-27 meta-repo this commit: 在 `openspec/changes/joint-mvp-gate-13301035545/tasks.md` 顶部加 audit banner 说明: § 1.1 pre-flight (`import aliyun_artc_pywrap`) 应在 dingrtc-migration § 7.5 落地后改读 `dingrtc_pywrap`; § 6 真拨号 13301035545 需 dingrtc-migration § 5.4 (cloud) + § 7.11 (Windows) + § 8.10 (macOS) 三端绿之后才能跑; AppId/AppKey 仍用同一对 (o6dpsan9 是 DingRTC 3.x, 凭据本身合法). 本 change **不自行 archive**, 等 dingrtc-migration archive 后, 11/43 剩余 32 task 继续推进 (真拨号 e2e 是 v1.0 MVP gate, 不归属 dingrtc-migration scope). `openspec validate joint-mvp-gate-13301035545 --strict` PASS. -->

## 12. memory / reference 文档更新

- [ ] 12.1 更新 [[reference_artc_sdk]]：整体重写，三端 SDK 路径换 DingRTC 3.9.0；保留各平台 binding 框架描述
- [ ] 12.2 更新 [[project_artc_join_diagnosis_2026_05_25]]：收尾段 "blocker B = 凭据" 改成 "blocker = 产品线找错；换 SDK 收敛"
- [ ] 12.3 更新 [[project_a2_path]] / [[project_a2_progress]] / [[project_a2_qa_deploy]]：所有引用 `AliRTCSDK_Linux` 处全部换成 DingRTC 等价
- [ ] 12.4 更新 [[project_macos_dev_path]]：macOS dev SDK 路径换 DingRTC.framework；env var 名换 `ISALES_MACOS_DINGRTC_FRAMEWORK_PATH`
- [ ] 12.5 更新 [[project_windows_artc_pybind11]]：active change 状态 + vendor 调用层重做实证 commit
- [ ] 12.6 更新 [[feedback_artc_vs_rtc_naming]]：补一条"ApsaraVideo Live 产品线下也有 ARTC SDK，是另一条独立产品，不跟 RTC PaaS 互通；iSales 一直用错了这条 Live 的 ARTC SDK"
- [ ] 12.7 更新 [[feedback_isolate_with_vendor_sample]]：加 best practice "下 vendor SDK 时核对**下载域名**对应的产品线，`alivc-demo-cms.alicdn.com` ≠ `dingrtc.oss-cn-zhangjiakou.aliyuncs.com`"
- [ ] 12.8 删除 / 失效 [[project_dingrtc_migration_groundtruth]]（本 change archive 后该 memory 内容已落入 archived change，memory 改成"DingRTC 已落地，详见 archive `<date>-engine-rtc-dingrtc-migration`"短句）

## 13. 验收 gate + archive

- [ ] 13.1 `openspec validate engine-rtc-dingrtc-migration --strict` PASS
- [ ] 13.2 三端 e2e 全绿 checklist：cloud `ecs_pcm_loopback_listen.py` 10s 回环 / Windows edge + cloud 真房间互通 / mac dev + cloud 真房间互通 / 拨真手机 13301035545 听到 AI 开场白
- [ ] 13.3 三端 vendor 版本一致 lint PASS（§ 10.1）
- [ ] 13.4 三端 STATE.md / RUNBOOK 同步验证（`grep -i AliRTC\|aliyun-artc\|AliVCSDK` 不应找到任何遗留 ApsaraVideo Live 引用，除 archived change 历史记录）
- [ ] 13.5 archive：`/opsx:archive engine-rtc-dingrtc-migration` → 合 delta 到 `openspec/specs/{device-hardware,deployment-topology,service-communication}/spec.md`
- [ ] 13.6 archive 后 `git push origin main`（meta-repo）+ 涉及 sub-repo（`isales-engine` / `isales-telephony` / `isales-common` 如改）+ 12.* memory 更新提交
